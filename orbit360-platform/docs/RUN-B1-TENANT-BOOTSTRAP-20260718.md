# Bloque 1 · Run oficial con bootstrap tenant genérico

Gate: `block1-client360-insurers-lab-v20260717`.

Preflight vinculante fusionado: `GO_GATE_CONTRACT`, 781 controles aprobados y 0 fallidos.

El shell carga el núcleo genérico, el índice de tenants y el helper del subsistema Router. El helper resuelve únicamente la configuración activa indicada por el índice antes de iniciar la validación de readiness del Router.

Ejecución única. M1 cierra exclusivamente con evidencia sanitizada `ok:true`.

Alcance: LAB; sin producción, merge, main, reimportación ni cambios en Store, Auth, reglas, datos o renderers.
