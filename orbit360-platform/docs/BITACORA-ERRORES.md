# Bitacora de Errores

## 2026-06-30

- `firebase --version` fallo en PowerShell porque el shim `firebase.ps1` esta bloqueado por ExecutionPolicy.
- Se uso `firebase.cmd` como alternativa segura para lectura de configuracion LAB.
- No se registraron errores de Firebase CLI al consultar `apps:list` ni `apps:sdkconfig`.

## Pendiente de navegador

- Registrar aqui cualquier error de consola durante login/logout con usuario ficticio DEV/LAB.
