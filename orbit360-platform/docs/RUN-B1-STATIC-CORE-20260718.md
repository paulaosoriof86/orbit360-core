# Bloque 1 · Run oficial con núcleo genérico precargado

Gate: `block1-client360-insurers-lab-v20260717`.

El workflow debe aprobar primero su preflight vinculante. Después valida el runtime con el core genérico y el índice de tenants cargados antes del Router.

Ejecución única. M1 cierra exclusivamente con evidencia sanitizada `ok:true`.

Alcance: LAB; sin producción, merge, main, reimportación ni cambios en Store, Auth, reglas, datos o renderers.
