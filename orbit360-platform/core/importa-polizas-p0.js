/* Orbit 360 · P0 reglas de importacion de polizas · 2026-07-30 */
(function(){
  window.Orbit=window.Orbit||{};
  const PAIS_MONEDA={GT:'GTQ',CO:'COP'};
  function norm(v){return String(v==null?'':v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9 ]/g,' ').replace(/\s+/g,' ').trim();}
  function parseNum(v){if(v==null||v==='')return 0;let s=String(v).replace(/[^0-9,.\-]/g,'').replace(/\.(?=\d{3}(\D|$))/g,'').replace(',','.');const n=parseFloat(s);return Number.isFinite(n)?n:0;}
  function dateYMD(v){if(!v)return'';const raw=String(v).trim();let m=raw.match(/^(\d{4})[-/](\d{1,2})[-/](\d{1,2})/);if(m)return`${m[1]}-${String(m[2]).padStart(2,'0')}-${String(m[3]).padStart(2,'0')}`;m=raw.match(/^(\d{1,2})[-/](\d{1,2})[-/](\d{4})/);return m?`${m[3]}-${String(m[2]).padStart(2,'0')}-${String(m[1]).padStart(2,'0')}`:raw.slice(0,10);}
  function todayYMD(){return Orbit.ui&&Orbit.ui.today?Orbit.ui.today():new Date().toISOString().slice(0,10);}
  function isActiveTerm(p,today){const now=today||todayYMD(),start=dateYMD(p.vigenciaIni||p.vigenciaInicio||p.desde),end=dateYMD(p.vigenciaFin||p.vigenciaFinal||p.hasta||p.vencimiento);if(start&&now<start)return false;if(end&&now>end)return false;return!!(start||end);}
  function resolveCountry(raw,ins){const n=norm(raw||(ins&&ins.pais)||'');if(n==='gt'||n==='gtm'||n.includes('guatemala'))return{country:'GT',source:raw?'row':'insurer',requiresValidation:false};if(n==='co'||n==='col'||n.includes('colombia'))return{country:'CO',source:raw?'row':'insurer',requiresValidation:false};return{country:'',source:'',requiresValidation:true};}
  function resolveCurrency(raw,country,ins){const n=norm(raw||(ins&&ins.moneda)||'');if(n==='gtq'||n.includes('quetzal'))return{currency:'GTQ',source:raw?'row':'insurer',requiresValidation:false};if(n==='cop'||n.includes('peso'))return{currency:'COP',source:raw?'row':'insurer',requiresValidation:false};if(n==='usd'||n.includes('dolar'))return{currency:'USD',source:raw?'row':'insurer',requiresValidation:false};if(country&&PAIS_MONEDA[country])return{currency:PAIS_MONEDA[country],source:'country',requiresValidation:false};return{currency:'',source:'',requiresValidation:true};}
  function splitPremium(i){const pn=parseNum(i.primaNeta||i.neta||i.prima_base),g=parseNum(i.gastos||i.gastosEmision||i.derechos),iva=parseNum(i.iva||i.impuesto||i.impuestos),pt=parseNum(i.primaTotal||i.total||i.primaBruta||i.prima);let net=pn,total=pt,ambiguous=false;if(!net&&total&&(g||iva))net=Math.max(0,total-g-iva);if(!total&&net)total=net+g+iva;if(!net&&total&&!g&&!iva)ambiguous=true;return{primaNeta:net,gastos:g,iva,primaTotal:total||net,ambiguous};}
  function operationalStatus(i,today){const s=norm(i.estadoFuenteOriginal||i.estadoPol||i.estado||i.status||''),active=isActiveTerm(i,today);if(/cancel|anulad|rescind/.test(s))return{estadoOperativoOrbit:'cancelada_terminal',estadoCartera:'no_exigible',label:'Cancelada',requiresValidation:false};if(/no renov/.test(s))return{estadoOperativoOrbit:'no_renovada_historica',estadoCartera:'no_exigible',label:'No renovada',requiresValidation:false};if(/^por renovar$/.test(s))return active?{estadoOperativoOrbit:'por_renovar_operativa',estadoCartera:'genera_recibos_esperados',label:'Por renovar',requiresValidation:false}:{estadoOperativoOrbit:'por_renovar_requiere_validacion',estadoCartera:'requiere_validacion',label:'Por renovar · requiere validación',requiresValidation:true};if(/renovad/.test(s))return{estadoOperativoOrbit:'historica_renovada',estadoCartera:'no_exigible',label:'Renovada',requiresValidation:false};if(/^vigente$|\bvigente\b/.test(s))return active?{estadoOperativoOrbit:'vigente_operativa',estadoCartera:'genera_recibos_esperados',label:'Vigente',requiresValidation:false}:{estadoOperativoOrbit:'vigente_fuera_vigencia_requiere_validacion',estadoCartera:'requiere_validacion',label:'Vigente · requiere validación',requiresValidation:true};if(/venc|termin|reexped/.test(s))return{estadoOperativoOrbit:'historica_vencida',estadoCartera:'recibo_analitico_no_cartera_viva',label:'Histórica',requiresValidation:false};return active?{estadoOperativoOrbit:'requiere_validacion_estado',estadoCartera:'requiere_validacion',label:'Requiere validación',requiresValidation:true}:{estadoOperativoOrbit:'historica_estado_no_activo',estadoCartera:'no_exigible',label:'Histórica',requiresValidation:false};}
  function partyKey(i){return i.clienteId||i.aseguradoId||i.contratanteId||i.tomadorId||i.identificacion||i.documento||i.clienteNombre||i.aseguradoNombre||i.contratanteNombre||i.tomadorNombre||i.nombre||'';}
  function policyDedupKey(i,resolvedCountry){const country=resolvedCountry||i.pais||i.country||'',ins=i.aseguradoraId||i.aseguradoraNombre||i.aseguradora||'',num=i.numero||i.poliza||i.numeroPoliza||'',party=partyKey(i);return country&&ins&&num&&party?[norm(country),norm(ins),norm(num),norm(party)].join('|'):'';}
  function policySourceVersionKey(i,resolvedCountry){const k=policyDedupKey(i,resolvedCountry),start=dateYMD(i.vigenciaIni||i.vigenciaInicio||i.desde),end=dateYMD(i.vigenciaFin||i.vigenciaFinal||i.hasta||i.vencimiento);return k&&start&&end?[k,start,end].join('|'):'';}
  function normalizePolicy(input,ctx){const insurer=(ctx&&ctx.insurer)||input.aseguradoraObj||null,c=resolveCountry(input.pais||input.country,insurer),m=resolveCurrency(input.moneda||input.divisa||input.currency,c.country,insurer),premium=splitPremium(input),status=operationalStatus(input,ctx&&ctx.today),dedup=policyDedupKey(input,c.country),version=policySourceVersionKey(input,c.country),forma=input.formaPago||input.frecuencia||input.periodicidad||input.conducto||'',missing=Array.isArray(input.motivosValidacion)?input.motivosValidacion.slice():[];const need=r=>{if(r&&!missing.includes(r))missing.push(r);};if(!dedup)need('llave_poliza');if(!version)need('vigencia');if(!c.country)need('pais');if(!m.currency)need('moneda');if(premium.ambiguous||!premium.primaNeta)need('prima_neta');if(!forma)need('forma_pago');if(status.requiresValidation)need('estado');return Object.assign({},input,{_dedupKey:dedup,_sourceVersionKey:version,estadoFuenteOriginal:input.estadoFuenteOriginal||input.estadoPol||input.estado||input.status||'',estadoOperativoOrbit:status.estadoOperativoOrbit,estadoCartera:status.estadoCartera,estadoConciliacion:input.estadoConciliacion||'pendiente',estado:status.label,pais:c.country,moneda:m.currency,monedaFuente:m.source,primaNeta:premium.primaNeta,gastos:premium.gastos,iva:premium.iva,primaTotal:premium.primaTotal,formaPago:forma,requiereValidacion:missing.length>0||input.requiereValidacion===true,motivosValidacion:missing,importadorP0:true});}
  function shouldGenerateExpectedReceipts(p){return!p.requiereValidacion&&(p.estadoOperativoOrbit==='vigente_operativa'||p.estadoOperativoOrbit==='por_renovar_operativa')&&!!p.primaNeta&&!!p.moneda&&!!p.formaPago;}
  function expectedReceiptSeed(p,r,index){return{id:r&&r.id?r.id:`rec_esp_${p.id||p._dedupKey}_${index}`,polizaId:p.id||'',clienteId:p.clienteId||'',asesorId:p.asesorId||'',cuota:r&&r.n!=null?r.n:index+1,monto:r&&r.total!=null?r.total:0,moneda:p.moneda,neta:r&&r.neta!=null?r.neta:0,gastosEmision:r&&r.gastosEmision!=null?r.gastosEmision:0,gastosFinan:r&&r.gastosFinan!=null?r.gastosFinan:0,otros:r&&r.otros!=null?r.otros:0,iva:r&&r.iva!=null?r.iva:0,vence:r&&r.vence?r.vence:'',fechaLimite:r&&r.fechaLimite?r.fechaLimite:'',fechaPago:null,estado:'esperado',estadoCartera:'recibo_esperado',estadoConciliacion:'pendiente',confirmadoPago:false,carteraOperativa:false,conciliado:false,origen:'poliza_importada',importado:true};}
  Orbit.importaPolizasP0={norm,parseNum,dateYMD,isActiveTerm,resolveCountry,resolveCurrency,splitPremium,operationalStatus,policyDedupKey,policySourceVersionKey,normalizePolicy,shouldGenerateExpectedReceipts,expectedReceiptSeed};
})();
