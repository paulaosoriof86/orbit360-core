# A&S CotComp — PREIMPL-03 S01 QA Evidence

**Fecha:** 27 de agosto de 2026  
**Scope:** `PREIMPL-03-S01 — executable schemas + validators + contracts + tests`  
**Rama:** `ays/cotcomp-preimpl03-entry-20260826`  
**Baseline congelada:** `e3504dcc35a6b48bdf2ea2baaa08dc516d69aa6b`  
**Estado QA:** `PASS`

## 1. Alcance verificado

S01 permanece source-only. No se autorizaron ni ejecutaron provider writes, Firestore/Storage/database writes, Auth/rules changes, adapters reales, notificaciones reales, deploy, staging, producción ni AUTO.

## 2. Archivos CotComp sometidos a QA

Los contenidos ejecutados fueron reconstruidos desde readback de la rama técnica y se verificó identidad mediante Git blob hash antes de ejecutar:

| Archivo | Git blob SHA verificado |
|---|---|
| `functions/cotcomp-contracts.js` | `2323b14546860add2fb527255056e21150e8c755` |
| `functions/cotcomp-validator.js` | `2132575b1eaa130724f89633181c4fe9a20fba77` |
| `functions/cotcomp-validator.test.js` | `907397366608e614f0e7c28b187eeda61c57e147` |
| `functions/cotcomp-proposal-contracts.js` | `34f83bfaf8f087bcbdd5d6ad8cf8676bd9dc8621` |
| `functions/cotcomp-proposal-contracts.test.js` | `ece65c843a3f3d6c90f73b0642d69110079987f0` |
| `functions/package.json` | `40799f3aa5e9f7f461a10bde2fbb1a33b93bc970` |

## 3. Entorno y ejecución

- Node: `v22.16.0`.
- Harness: entorno aislado sin provider/network/data writes.
- Syntax checks ejecutados:
  - `node --check cotcomp-contracts.js`
  - `node --check cotcomp-validator.js`
  - `node --check cotcomp-validator.test.js`
  - `node --check cotcomp-proposal-contracts.js`
  - `node --check cotcomp-proposal-contracts.test.js`
- Resultado syntax: `PASS 5/5`.
- Suite ejecutada: `node --test cotcomp-validator.test.js cotcomp-proposal-contracts.test.js`.
- Resultado: `27 tests / 27 PASS / 0 FAIL / 0 skipped / 0 cancelled`.

No se usa inspección visual como sustituto de ejecución.

## 4. Cobertura contractual demostrada

La suite demuestra, entre otros:

- payloads válidos de los cuatro journeys MVP;
- requiredness y requiredness condicional;
- enums dependientes por journey;
- restricciones de dependientes y fechas;
- país/journey consistency;
- `idempotencyKey` obligatorio;
- consentimiento obligatorio y alias controlado;
- rechazo de campos diferidos/no públicos;
- `AUTO_READY=false` y default-deny;
- Proposal con estructura mínima congelada;
- Proposal `VALIDATED` requiere provenance de validación;
- comparación solo para Proposal validada y con vigencia actual confirmada;
- `MISSING` no equivale a `NOT_COVERED`;
- `NOT_COVERED` requiere declaración explícita de fuente;
- sin ranking, weighting silencioso ni inferencia de pricing histórico por defecto.

## 5. Diff / HARD PRESERVE

El diff final debe permanecer limitado a archivos `functions/cotcomp-*`, este artefacto técnico y el cambio mínimo de scripts en `functions/package.json`.

Cualquier aparición de reglas Firebase, Auth, provider/config, control-plane, go-live, datos, módulos de cobros/pagos/comisiones, Portal o frontend invalida este PASS y obliga a volver al gate.

## 6. Veredicto

`PREIMPL-03-S01_QA = PASS` condicionado al readback/diff final de rama contra la baseline congelada.

El PASS de S01 no autoriza runtime side effects, adapters, AUTO, deploy ni producción.
