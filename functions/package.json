{
  "name": "orbit360-secure-functions",
  "version": "1.5.0",
  "private": true,
  "description": "Orbit 360 secure multi-tenant backend for onboarding, self-service configuration, workflows, advisor inbox, recurring imports, reconciliation and operational resources.",
  "main": "bootstrap.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check user-onboarding.js && node --check tenant-domain-config.js && node --check ops-leads-domain.js && node --check ops-advisor-inbox.js && node --check cobros-reconciliation-domain.js && node --check recurring-insurance-import.js && node --check index.js && node --check bank-accounts.js && node --check bootstrap.js && node --check cotcomp-contracts.js && node --check cotcomp-validator.js && node --check cotcomp-validator.test.js && node --check cotcomp-proposal-contracts.js && node --check cotcomp-proposal-contracts.test.js",
    "test:cotcomp": "node --test cotcomp-validator.test.js cotcomp-proposal-contracts.test.js"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "6.2.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "7.3.0"
  }
}
