{
  "name": "orbit360-secure-functions",
  "version": "1.3.0",
  "private": true,
  "description": "Orbit 360 secure multi-tenant backend for onboarding, workflows, reconciliation and operational resources.",
  "main": "bootstrap.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check user-onboarding.js && node --check ops-leads-domain.js && node --check cobros-reconciliation-domain.js && node --check index.js && node --check bank-accounts.js && node --check bootstrap.js"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "6.2.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "7.3.0"
  }
}
