name: Orbit 360 · M5 Hosting LAB delivery 5.0.32 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Hosting authorization already consumed
        run: |
          echo 'M5 5.0.32 Hosting LAB delivery already executed exactly once; rerun forbidden.' >&2
          exit 41
