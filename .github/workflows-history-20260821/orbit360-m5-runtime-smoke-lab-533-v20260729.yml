name: Orbit 360 · M5 runtime smoke LAB · 5.0.33 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Runtime 5.0.33 consumed
        shell: bash
        run: |
          echo 'M5 runtime 5.0.33 one-shot authorization was consumed by run 30495235821.'
          echo 'No rerun or second request is permitted. Static root-cause remediation is required first.'
          exit 41
