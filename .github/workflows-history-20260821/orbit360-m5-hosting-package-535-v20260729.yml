name: Orbit 360 · M5 Hosting package 5.0.35 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Package already closed
        run: |
          echo 'M5 5.0.35 package already PASS; rerun forbidden.' >&2
          exit 41
