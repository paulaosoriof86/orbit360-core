name: Orbit 360 · M5 5.0.37 seed patch · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Superseded mechanism
        run: |
          echo 'Direct seed patch mechanism was not executed and is superseded by the first loaded Academia bridge normalization; rerun forbidden.' >&2
          exit 41
