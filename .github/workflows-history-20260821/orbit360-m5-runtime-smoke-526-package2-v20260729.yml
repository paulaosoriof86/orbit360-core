name: Orbit 360 · M5 5.0.26 · FROZEN package stop-line

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop-line contractual
        run: |
          echo 'M5 5.0.26 stopped after two package pipeline failures; third package attempt forbidden.'
          exit 41
