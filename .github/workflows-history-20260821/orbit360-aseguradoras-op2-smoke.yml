name: FROZEN · Orbit 360 Directorios Aseguradoras LAB

# Hard freeze after repeated methodology loop on 2026-07-21.
# No automatic trigger and no runtime execution are permitted until the
# methodology review is closed in issue #7 and the incident freeze is lifted.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-methodology-review:
    if: ${{ false }}
    name: FROZEN · no ejecutar directorios, bóveda ni gate
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "STOP_THE_LINE_METHODOLOGY_REVIEW"
          echo "See tools/orbit360-incident-freeze-v20260721.json"
          exit 1
