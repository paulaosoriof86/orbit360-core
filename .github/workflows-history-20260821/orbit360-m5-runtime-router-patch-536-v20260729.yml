name: Orbit 360 · M5 canonical router binding · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Router already bound
        run: |
          echo 'Canonical M5 gate already bound to closed static producer inventory 5.0.37; rerun forbidden.' >&2
          exit 41
