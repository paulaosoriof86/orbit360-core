name: Orbit 360 · M5 runtime preparation 5.0.39 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Runtime preparation already closed
        run: |
          echo 'M5 5.0.39 runtime preparation already PASS; rerun forbidden.' >&2
          exit 41
