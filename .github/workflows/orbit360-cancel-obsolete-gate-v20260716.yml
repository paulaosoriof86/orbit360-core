name: Orbit 360 Importers E2E Frozen After Confirmation Failures 20260720

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen_contract:
    name: Importadores · freeze de sesión legal pendiente
    runs-on: ubuntu-latest
    env:
      ORBIT360_GATE_ID: importers-e2e-acceptance-lab-v20260720
      FIREBASE_PROJECT_ID: ays-orbit-360-lab
      ORBIT360_HOSTING_CHANNEL: orbit360-ays-lab
      ORBIT360_PREFLIGHT_EVIDENCE: orbit360-platform/runtime-gate-crm-v20260716/preflight-sanitizado.json
    steps:
      - name: Checkout rama autorizada
        uses: actions/checkout@v4
        with:
          ref: ays/backend-tenant-lab-v99-20260703

      - name: Confirmar freeze
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_REF_NAME}" = "ays/backend-tenant-lab-v99-20260703"
          echo "FREEZE_IMPORTERS_E2E_CONFIRMATION_STAGE"
          echo "Root cause: synthetic browser session did not satisfy the mandatory legal acceptance gate before the first actionable click."
          echo "No secrets, browser, deploy, fixture or data writes are executed by this frozen workflow."
