name: Orbit 360 · M5 runtime 5.0.25 · FROZEN 5.0.26 stop-line

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop-line contractual
        run: |
          echo 'Runtime 5.0.25 request is forbidden under wrapper 5.0.26 after two package pipeline failures.'
          exit 41
