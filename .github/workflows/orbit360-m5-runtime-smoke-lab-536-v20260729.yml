name: Orbit 360 · M5 runtime smoke LAB · 5.0.36 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Runtime authorization consumed
        run: |
          echo 'M5 runtime 5.0.36 was executed exactly once in run 30500309087; rerun forbidden.' >&2
          exit 41
