name: Orbit 360 · M5 Hosting LAB delivery 5.0.30 · FROZEN
run-name: M5 5.0.30 · FROZEN · ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    name: STOP · two package pipeline failures
    runs-on: ubuntu-24.04
    steps:
      - name: Bloqueo vinculante
        shell: bash
        run: |
          echo 'M5 5.0.30 STOPPED_AFTER_TWO_PACKAGE_PIPELINE_FAILURES'
          echo 'No package, request or Hosting retry is authorized.'
          echo 'Required next step: new zero-capability static Hosting control-plane redesign.'
          exit 1
