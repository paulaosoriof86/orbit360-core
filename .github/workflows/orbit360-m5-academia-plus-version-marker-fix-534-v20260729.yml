name: Orbit 360 · M5 5.0.34 Academia static marker fix · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Source remediation already applied
        shell: bash
        run: |
          echo 'Academia PLUS version-marker remediation was already applied exactly once.'
          echo 'No patch rerun is permitted.'
          exit 41
