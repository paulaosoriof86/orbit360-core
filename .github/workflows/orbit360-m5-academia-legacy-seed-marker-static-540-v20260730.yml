name: Orbit 360 · M5 5.0.40 legacy seed marker static validation · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Static remediation already closed
        run: |
          echo 'M5 5.0.40 static validation already PASS; rerun forbidden.' >&2
          exit 41
