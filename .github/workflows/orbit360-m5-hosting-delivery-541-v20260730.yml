name: Orbit 360 · M5 Hosting LAB delivery 5.0.41 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Hosting delivery already closed
        run: |
          echo 'M5 Hosting LAB 5.0.41 already delivered once and verified 29/29; rerun forbidden.' >&2
          exit 41
