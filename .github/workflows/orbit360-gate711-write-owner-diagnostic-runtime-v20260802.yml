name: Orbit 360 · Gate 7.11 · root fix Academia · workflow anterior cerrado
on:
  push:
    branches: [ays/backend-tenant-lab-v99-20260703]
    paths:
      - '.github/orbit360-closed/gate711-academia-rootfix-ledger-old-no-replay'
permissions:
  contents: read
jobs:
  closed:
    name: Request con ledger anterior consumido
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'DATA_CONTRACT_FAILURE_CLOSED_BY_MANIFEST_RESEAL'
          echo 'No runtime replay allowed.'
