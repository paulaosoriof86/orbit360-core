name: Orbit360 Continuity Canonical Source Only
# MACRO3_SOURCE_REPAIR_FREEZE_20260823_V2
# Fail-closed while the convergence validator is generalized from Macro-2 assumptions to Macro-3 derived state.

on:
  workflow_dispatch:
    inputs:
      transition:
        description: 'INERT_SOURCE_REPAIR_FREEZE'
        required: true
        type: string

permissions:
  contents: read
  actions: read

jobs:
  source-repair-freeze:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'MACRO3_SOURCE_REPAIR_FREEZE_V2'
          echo 'No runtime. Convergence validator synchronization in progress.'
          exit 41
