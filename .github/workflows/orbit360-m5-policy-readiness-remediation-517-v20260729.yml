# FROZEN 2026-07-29: two failures on the same static stage; no third attempt.
name: Orbit 360 · M5 policy readiness remediation · 5.0.17 · frozen
run-name: M5 5.0.17 · frozen · ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  frozen:
    name: M5 5.0.17 · frozen by two-failure rule
    runs-on: ubuntu-24.04
    steps:
      - name: Stop frozen workflow
        shell: bash
        run: |
          echo 'M5 5.0.17 is frozen after two static pipeline failures.'
          echo 'Use the new validator-redesign block; do not rerun 5.0.17.'
          exit 41
