name: Orbit 360 · M5 5.0.37 fixture diagnostic · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Diagnostic already closed
        run: |
          echo 'M5 5.0.37 fixture diagnostic closed after root cause was isolated; rerun forbidden.' >&2
          exit 41
