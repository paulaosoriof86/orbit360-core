name: Orbit 360 · M5 Hosting 5.0.32 closure verification · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Closure already verified
        run: |
          echo 'M5 5.0.32 closure already verified PASS; rerun forbidden.' >&2
          exit 41
