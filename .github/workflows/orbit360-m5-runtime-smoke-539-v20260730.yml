name: Orbit 360 · M5 runtime smoke LAB · 5.0.39 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Runtime one-shot already consumed
        run: |
          echo 'M5 runtime 5.0.39 one-shot authorization already consumed; rerun forbidden.' >&2
          exit 41
