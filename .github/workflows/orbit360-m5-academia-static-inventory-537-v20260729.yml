name: Orbit 360 · M5 5.0.37 Academia producer inventory · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Static inventory already closed
        run: |
          echo 'M5 5.0.37 static inventory closed PASS in run 30501531194; rerun forbidden.' >&2
          exit 41
