name: FROZEN · Orbit 360 Incident Importador Aseguradoras

# Hard freeze after repeated methodology loop on 2026-07-21.
# No push/pull_request trigger. No runtime, secrets, Firestore, vault or recovery.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen-methodology-review:
    if: ${{ false }}
    name: FROZEN · revisión metodológica requerida
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "STOP_THE_LINE_METHODOLOGY_REVIEW"
          echo "See tools/orbit360-incident-freeze-v20260721.json"
          exit 1
