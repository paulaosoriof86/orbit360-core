name: Orbit 360 · Gate 7.11 · root fix Academia runtime · CERRADO
on:
  push:
    branches: [ays/backend-tenant-lab-v99-20260703]
    paths:
      - '.github/orbit360-closed/gate711-academia-rootfix-runtime-no-replay'
permissions:
  contents: read
jobs:
  closed:
    name: Runtime cerrado por STOP_RETRY
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'CANONICAL_RUNTIME_CUMULATIVE_VISUAL_LAB_STOP_RETRY'
          echo 'Root cause fixed and statically ready; no runtime replay is allowed.'
