name: Orbit 360 · M5 5.0.40 legacy Academia seed marker fix · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy seed marker fix already applied
        run: |
          echo 'M5 5.0.40 legacy seed marker correction already applied and statically validated; rerun forbidden.' >&2
          exit 41
