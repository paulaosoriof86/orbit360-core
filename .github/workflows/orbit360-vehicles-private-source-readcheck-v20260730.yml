name: Orbit 360 · Vehicles private sources readcheck · historical
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  historical_guard:
    name: Vehicles · historical source-read closure guard
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Confirmar cierre histórico sin secrets ni fuentes privadas
        shell: bash
        run: |
          set -euo pipefail
          test -f orbit360-platform/docs/CIERRE-WRITE-VEHICULOS-AYS-20260730.md
          grep -Fq 'Estado: `WRITE_PASS`' orbit360-platform/docs/CIERRE-WRITE-VEHICULOS-AYS-20260730.md
          grep -Fq 'vehiculos: 1032' orbit360-platform/docs/CIERRE-WRITE-VEHICULOS-AYS-20260730.md
          grep -Fq 'La autorización fue consumida una sola vez' orbit360-platform/docs/CIERRE-WRITE-VEHICULOS-AYS-20260730.md
          echo 'HISTORICAL_SOURCE_READCHECK_RETIRED_AFTER_WRITE_PASS'
