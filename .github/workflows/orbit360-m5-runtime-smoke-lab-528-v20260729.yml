name: Orbit 360 · M5 runtime smoke LAB · 5.0.28 FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Stop-line 5.0.28 consumed
        shell: bash
        run: |
          echo 'M5 5.0.28 is closed. The one-shot runtime authorization was consumed by run 30486294373.'
          echo 'No rerun is permitted. Continue only through the post-failure static root-cause remediation.'
          exit 41
