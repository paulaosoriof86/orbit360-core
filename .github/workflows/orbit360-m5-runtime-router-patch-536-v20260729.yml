name: Orbit 360 · M5 5.0.36 canonical router patch · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Router stop patch already applied
        run: |
          echo 'Canonical M5 gate already returned to zero-capability stop state after runtime 5.0.36; rerun forbidden.' >&2
          exit 41
