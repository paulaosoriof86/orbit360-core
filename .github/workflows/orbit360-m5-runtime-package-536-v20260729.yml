name: Orbit 360 · M5 runtime package 5.0.36 · FROZEN

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Runtime package already closed
        run: |
          echo 'M5 runtime 5.0.36 package already PASS in run 30499967324; rerun forbidden.' >&2
          exit 41
