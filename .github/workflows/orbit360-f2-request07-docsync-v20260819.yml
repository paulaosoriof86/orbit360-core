name: Orbit360 F2 Request07 Canonical Docs Sync 20260819
on:
  push:
    branches: [ays/backend-tenant-lab-v99-20260703]
    paths:
      - '.github/orbit360-requests/f2-request07-docsync-20260819.json'
permissions:
  contents: write
concurrency:
  group: orbit360-f2-request07-docsync-v20260819
  cancel-in-progress: false
env:
  ORBIT360_BRANCH: ays/backend-tenant-lab-v99-20260703
  TRIGGER: .github/orbit360-requests/f2-request07-docsync-20260819.json
  LIVE: orbit360-platform/docs/orbit360-live-state-v1.json
  INDEX: orbit360-platform/docs/ORBIT360-CURRENT-DOCUMENTATION-INDEX-v1.json
  CHECKPOINT: orbit360-platform/docs/CHECKPOINT-F2-REQUEST07-VALIDATOR-STALE-SOURCEFIX-PASS-REQUEST08-AUTH-PENDING-20260819.md
  ACADEMIA: orbit360-platform/docs/ACADEMIA-ACTUALIZACION-F2-REQUEST07-VALIDATOR-STALE-20260819.md
  SOURCEFIX_EVIDENCE: orbit360-platform/runtime-gate-crm-v20260716/f2-request07-validator-stale-sourcefix-check-v20260819.json
  DISCOVERY_EVIDENCE: orbit360-platform/runtime-gate-crm-v20260716/f2-request07-runtime-discovery-v20260819.json
  DOCSYNC_EVIDENCE: orbit360-platform/runtime-gate-crm-v20260716/f2-request07-canonical-docsync-v20260819.json
jobs:
  docs_sync:
    runs-on: ubuntu-24.04
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate canonical docs-sync boundary
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "$ORBIT360_BRANCH"
          mapfile -t CHANGED < <(git diff-tree --no-commit-id --name-only -r HEAD)
          test "${#CHANGED[@]}" = '1'
          test "${CHANGED[0]}" = "$TRIGGER"
          jq -e '.schemaVersion=="orbit360-f2-request07-docsync-trigger-v1" and .request07RunId==32311320804 and .candidateArtifactId==9385306424 and .sourcefixEvidenceCommit=="1b9344944f3691ac40eb7face6d33c7034e0dd37" and .docsOnly==true and .secrets==false and .browser==false and .runtime==false and .dataAccess==false and .writes==false and .deploy==false and .production==false' "$TRIGGER" >/dev/null
          jq -e '.ok==true and .status=="F2_REQUEST07_VALIDATOR_STALE_SOURCEFIX_PASS" and .candidateArtifactId==9385306424 and .request07RunId==32311320804 and .runtimeExecuted==false and .firestoreWrites==0 and .authWrites==0 and .dataWrites==0' "$SOURCEFIX_EVIDENCE" >/dev/null
          jq -e '.status=="TARGET_RUN_TERMINAL_FAILURE" and .targetRunId==32311320804 and .requestCommit=="696b3700687f758fdd9c629a2268caa857aa377a" and .multipleRuns==false' "$DISCOVERY_EVIDENCE" >/dev/null
          test -f "$CHECKPOINT"
          test -f "$ACADEMIA"
      - name: Synchronize live state and current documentation index
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs');
          const livePath=process.env.LIVE,indexPath=process.env.INDEX,checkpoint=process.env.CHECKPOINT,academia=process.env.ACADEMIA;
          const live=JSON.parse(fs.readFileSync(livePath,'utf8'));
          const idx=JSON.parse(fs.readFileSync(indexPath,'utf8'));
          const now=new Date().toISOString();
          const artifact=9385306424, source='b94b2ae86d26586a68d33be9edba8715e956b02e';
          const zip='orbit360-fase-a-product-f2-request06-rootfix-successor-b94b2ae86d26.zip';
          const zipSha='81a96f476fd0fdfd814b3f047951ce653fd324bef8a6d96d6ee6fe44dd7bdcf4';
          const manifestSha='cc6170121ed61fd6d9cde867dfcae8a3dd23d29777c6ee28c240d70e49843eef';
          live.stateVersion='20260819.f2.request07-validator-stale-sourcefix-pass-request08-auth-pending.current';
          live.updatedAt=now;
          live.phase='F2_REQUEST07_VALIDATOR_STALE_SOURCEFIX_PASS_REQUEST08_AUTH_PENDING';
          live.f2SourceOnly=live.f2SourceOnly||{};
          live.f2SourceOnly.requestCreated=true;
          live.f2SourceOnly.runtimeAuthorized=false;
          live.f2SourceOnly.browserAuthorized=false;
          live.rootCauseState=live.rootCauseState||{};
          live.rootCauseState.currentBlockingFact={code:'F2_REQUEST08_RUNTIME_FRESH_AUTHORIZATION_REQUIRED',status:'FRESH_AUTHORIZATION_REQUIRED',candidateArtifactId:artifact,priorRequest:'REQUEST07_CONSUMED',priorRunId:32311320804};
          live.rootCauseState.f2Request07ValidatorStale={classification:'VALIDATOR_STALE',code:'F2_BROWSER_MANIFEST_IDENTITY_EXPECTATION_BOUND_TO_PREDECESSOR',status:'CLOSED_SOURCE_ONLY_PASS',observedRuntimeClassification:'DATA_CONTRACT_FAILURE',reclassifiedAfterRootCause:true,requestCommit:'696b3700687f758fdd9c629a2268caa857aa377a',runId:32311320804,evidenceArtifactId:9386546905,candidateArtifactId:artifact,productAffected:false,dataAffected:false,runtimeActivated:false,integrityBeforeAfterPass:true,sourcefixEvidence:'orbit360-platform/runtime-gate-crm-v20260716/f2-request07-validator-stale-sourcefix-check-v20260819.json',sourcefixEvidenceCommit:'1b9344944f3691ac40eb7face6d33c7034e0dd37',preventiveControl:'DYNAMIC_IMMUTABLE_REQUEST_CANDIDATE_BINDING_IN_RUNTIME_RUNNER_AND_SELFTEST'};
          live.documentationControl=live.documentationControl||{};
          live.documentationControl.currentCheckpoint=checkpoint;
          live.documentationControl.transactionStatus='F2_REQUEST07_VALIDATOR_STALE_SOURCEFIX_PASS_REQUEST08_AUTH_PENDING';
          live.documentationControl.latestAcademiaUpdate=academia;
          live.frozenPlan=live.frozenPlan||{};
          live.frozenPlan.currentPhase='F2';
          live.frozenPlan.currentPhaseInternalPercent=0;
          live.frozenPlan.currentPhaseInternalMethod='request07_consumed_validator_stale_sourcefix_pass_request08_fresh_authorization_pending';
          live.frozenPlan.currentPhaseSubphases=live.frozenPlan.currentPhaseSubphases||{};
          live.frozenPlan.currentPhaseSubphases.F2_request07_runtime_browser_readonly_acceptance='CONSUMED_STOP_VALIDATOR_STALE_RUN_32311320804';
          live.frozenPlan.currentPhaseSubphases.F2_request07_validator_stale_sourcefix='CLOSED_SOURCE_ONLY_PASS_COMMIT_1b9344944f3691ac40eb7face6d33c7034e0dd37';
          live.lanes={A_frontend_UX:'FROZEN_ROOTFIX_CERTIFIED_SOURCE_CLOSED_PASS',B_backend_security_gates:'REQUEST07_CONSUMED_VALIDATOR_STALE_SOURCEFIX_PASS_REQUEST08_FRESH_AUTH_PENDING',C_real_data_migration:'UNTOUCHED_ZERO_CHANGES'};
          live.authorization=live.authorization||{};
          live.authorization.browserAuthorizedNow=false;
          live.authorization.runtimeAuthorizedNow=false;
          live.authorization.publicationAuthorizedNow=false;
          live.authorization.deployAuthorizedNow=false;
          live.authorization.f2AuthorizationStatus='FRESH_AUTHORIZATION_REQUIRED_REQUEST08_EXACT_ARTIFACT_9385306424';
          live.goLive=live.goLive||{};
          live.goLive.status='BLOCKED_F2_REQUEST08_FRESH_AUTHORIZATION_REQUIRED';
          live.goLive.successorPublished=false;
          live.goLive.productionOperationalDeclared=false;
          live.stopRetry=live.stopRetry||{};
          live.stopRetry.request07MayBeRerun=false;
          live.stopRetry.request07RequestReplayAllowed=false;
          live.stopRetry.request08MayBeCreatedWithoutFreshAuthorization=false;
          live.nextActionExact={action:'OBTAIN_FRESH_AUTHORIZATION_REQUEST08_RUNTIME_BROWSER_READONLY',gateId:'f2-productive-acceptance-exact-successor-v20260818',requestVersion:'F2_PRODUCTIVE_ACCEPTANCE_RUNTIME_BROWSER_READONLY_V1',candidateArtifactId:artifact,candidateSourceHead:source,status:'FRESH_AUTHORIZATION_REQUIRED',runtimeAuthorizationRequired:true,runtimeExecuted:false};
          live.resumeProtocol=[
            'Read ORBIT360-CURRENT-DOCUMENTATION-INDEX-v1.json',
            'Read orbit360-live-state-v1.json',
            'Confirm actual HEAD and PR #5 remain draft/open/unmerged',
            'Read CHECKPOINT-F2-REQUEST07-VALIDATOR-STALE-SOURCEFIX-PASS-REQUEST08-AUTH-PENDING-20260819.md',
            'Request07 is consumed: do not rerun or replay it',
            'Do not reopen Request06, authentication, password, membership existence, HostDime or data reimport as current blockers',
            'Keep candidate artifact 9385306424 frozen and unpublished',
            'Create Request08 only after fresh explicit authorization',
            'Run the canonical F2 gate before artifact/provider/secrets/Firestore/browser for Request08',
            'Do not deploy, publish, merge main, or perform writes under the Request08 runtime-readonly boundary'
          ];
          live.f2Runtime07={status:'CONSUMED_STOP_VALIDATOR_STALE',requestPath:'.github/orbit360-requests/f2-productive-acceptance-runtime-browser-readonly-runbound-20260819-07.json',requestCommit:'696b3700687f758fdd9c629a2268caa857aa377a',runId:32311320804,jobId:96254728198,runAttempt:1,conclusion:'failure',candidateArtifactId:artifact,candidateSourceHead:source,canonicalGateGo:true,artifactVerified:true,identityReadOnlyPass:true,browserExecuted:true,runtimeActivated:false,stopClassification:'VALIDATOR_STALE',stopCode:'F2_BROWSER_MANIFEST_IDENTITY_EXPECTATION_BOUND_TO_PREDECESSOR',observedClassification:'DATA_CONTRACT_FAILURE',integrityBeforeAfterPass:true,firestoreWrites:0,authWrites:0,membershipWrites:0,dataWrites:0,operationalWrites:0,deployExecuted:false,publicationExecuted:false,productionTouched:false,requestConsumed:true,replayAllowed:false,terminalArtifactId:9386546905,sourcefixStatus:'CLOSED_SOURCE_ONLY_PASS',sourcefixEvidenceCommit:'1b9344944f3691ac40eb7face6d33c7034e0dd37',sourcefixEvidence:'orbit360-platform/runtime-gate-crm-v20260716/f2-request07-validator-stale-sourcefix-check-v20260819.json'};
          idx.updatedAt=now;
          idx.operationalCurrent=idx.operationalCurrent||{};
          Object.assign(idx.operationalCurrent,{
            resumePointer:checkpoint,
            latestRuntimeEvidence:'orbit360-platform/runtime-gate-crm-v20260716/f2-request07-runtime-discovery-v20260819.json',
            latestTerminalEvidence:'orbit360-platform/runtime-gate-crm-v20260716/f2-request07-runtime-discovery-v20260819.json',
            latestRequestConsumptionEvidence:'orbit360-platform/runtime-gate-crm-v20260716/f2-request07-runtime-discovery-v20260819.json',
            latestValidatorSourcefixEvidence:'orbit360-platform/runtime-gate-crm-v20260716/f2-request07-validator-stale-sourcefix-check-v20260819.json',
            latestAcademiaUpdate:academia,
            currentCheckpoint:checkpoint,
            currentPhase:'F2_REQUEST07_VALIDATOR_STALE_SOURCEFIX_PASS_REQUEST08_AUTHORIZATION_PENDING',
            currentPhaseInternalPercent:0,
            currentPhaseInternalMethod:'request07_consumed_validator_stale_sourcefix_pass_request08_fresh_authorization_pending',
            goLiveRoutePercentClosed:50,
            integratedProgramPercentClosed:25,
            currentBlocker:'Fresh explicit authorization is required for Request08 after Request07 was consumed by a VALIDATOR_STALE manifest-identity expectation bound to the predecessor. Product candidate 9385306424 remains frozen; zero data/runtime writes occurred.',
            f2RuntimeRequestCreated:true,
            f2RuntimeAuthorizationGranted:false,
            successorCandidateArtifactId:artifact,
            successorEvidenceArtifactId:'9385307587',
            successorZip:zip,
            successorZipSha256:zipSha,
            successorManifestSha256:manifestSha,
            successorSourceHead:source,
            successorFileCount:194,
            successorPublished:false,
            nextAuthorizationBoundary:'FRESH_AUTHORIZATION_REQUIRED:F2_PRODUCTIVE_ACCEPTANCE_RUNTIME_BROWSER_READONLY_V1:REQUEST08:EXACT_ARTIFACT_9385306424',
            request06Created:true,
            request07Created:true,
            request07Commit:'696b3700687f758fdd9c629a2268caa857aa377a',
            request07RunId:'32311320804',
            request07JobId:'96254728198',
            request07Status:'CONSUMED_STOP_VALIDATOR_STALE',
            request07Consumed:true,
            request07ReplayAllowed:false,
            request07TerminalArtifactId:'9386546905',
            request07CanonicalGateGo:true,
            request07ArtifactVerified:true,
            request07IdentityReadOnlyPass:true,
            request07IntegrityBeforeAfterPass:true,
            request07BrowserExecuted:true,
            request07RuntimeActivated:false,
            request07RootCause:'VALIDATOR_STALE:F2_BROWSER_MANIFEST_IDENTITY_EXPECTATION_BOUND_TO_PREDECESSOR',
            request07ValidatorSourcefixStatus:'CLOSED_SOURCE_ONLY_PASS',
            request07ValidatorSourcefixEvidenceCommit:'1b9344944f3691ac40eb7face6d33c7034e0dd37',
            request08Created:false,
            request08AuthorizationGranted:false
          });
          fs.writeFileSync(livePath,JSON.stringify(live,null,2)+'\n');
          fs.writeFileSync(indexPath,JSON.stringify(idx,null,2)+'\n');
          NODE
          node -e "JSON.parse(require('fs').readFileSync(process.env.LIVE)); JSON.parse(require('fs').readFileSync(process.env.INDEX));"
          jq -e '.phase=="F2_REQUEST07_VALIDATOR_STALE_SOURCEFIX_PASS_REQUEST08_AUTH_PENDING" and .f2Runtime07.status=="CONSUMED_STOP_VALIDATOR_STALE" and .f2Runtime07.runId==32311320804 and .f2Runtime07.replayAllowed==false and .authorization.f2AuthorizationStatus=="FRESH_AUTHORIZATION_REQUIRED_REQUEST08_EXACT_ARTIFACT_9385306424" and .goLive.status=="BLOCKED_F2_REQUEST08_FRESH_AUTHORIZATION_REQUIRED" and .nextActionExact.action=="OBTAIN_FRESH_AUTHORIZATION_REQUEST08_RUNTIME_BROWSER_READONLY"' "$LIVE" >/dev/null
          jq -e '.operationalCurrent.currentPhase=="F2_REQUEST07_VALIDATOR_STALE_SOURCEFIX_PASS_REQUEST08_AUTHORIZATION_PENDING" and .operationalCurrent.request07Consumed==true and .operationalCurrent.request07ReplayAllowed==false and .operationalCurrent.request08Created==false and (.operationalCurrent.nextAuthorizationBoundary|contains("REQUEST08")) and .operationalCurrent.successorCandidateArtifactId==9385306424' "$INDEX" >/dev/null
      - name: Build sanitized docs-sync evidence
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$(dirname "$DOCSYNC_EVIDENCE")"
          jq -n --arg head "$GITHUB_SHA" --arg cp "$CHECKPOINT" '{schemaVersion:"orbit360-f2-request07-canonical-docsync-v1",ok:true,status:"F2_REQUEST07_CANONICAL_DOCSYNC_PASS",classification:"PASS",triggerHead:$head,request07RunId:32311320804,candidateArtifactId:9385306424,request07Consumed:true,request07ReplayAllowed:false,validatorStaleSourcefixPass:true,nextAuthorizationBoundary:"REQUEST08_FRESH_AUTHORIZATION_REQUIRED",checkpoint:$cp,liveStateSynchronized:true,currentIndexSynchronized:true,academiaSynchronized:true,productMutation:false,dataMutation:false,browserExecuted:false,runtimeExecuted:false,secretAccess:false,firestoreRead:false,firestoreWrites:0,authWrites:0,membershipWrites:0,dataWrites:0,operationalWrites:0,deployExecuted:false,publicationExecuted:false,productionTouched:false,containsPII:false,containsSecrets:false}' > "$DOCSYNC_EVIDENCE"
      - name: Persist canonical docs transaction
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'orbit360-docsync-bot'
          git config user.email 'orbit360-docsync-bot@users.noreply.github.com'
          git add "$LIVE" "$INDEX" "$DOCSYNC_EVIDENCE"
          git diff --cached --check
          git commit -m 'docs(f2): sync Request07 consumed and Request08 auth boundary [skip ci]'
          git fetch origin "$ORBIT360_BRANCH"
          git rebase "origin/$ORBIT360_BRANCH"
          git push origin "HEAD:$ORBIT360_BRANCH"
