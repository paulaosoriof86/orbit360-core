#!/usr/bin/env node
'use strict';
import fs from 'node:fs';import path from 'node:path';
const ROOT=process.cwd(),ROUTER=path.join(ROOT,'tools/orbit360-validar-gate-contracts-v20260717.mjs'),SOURCE='tools/orbit360-validator-lifecycle-contract-f2-productive-acceptance-source-v20260818.json',RUNTIME='tools/orbit360-validator-lifecycle-contract-f2-productive-acceptance-runtime-v20260818.json';let s=fs.readFileSync(ROUTER,'utf8');const oldToken=`lifecycle: '${SOURCE}'`,newToken=`lifecycle: '${RUNTIME}'`,esc=x=>x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),oldCount=(s.match(new RegExp(esc(oldToken),'g'))||[]).length,newCount=(s.match(new RegExp(esc(newToken),'g'))||[]).length;if(oldCount===1&&newCount===0)s=s.replace(oldToken,newToken);else if(!(oldCount===0&&newCount===1))throw new Error(`F2_RUNTIME_REGISTER_LIFECYCLE_COUNT_INVALID:${oldCount}/${newCount}`);fs.writeFileSync(ROUTER,s,'utf8');console.log(JSON.stringify({ok:true,status:'F2_RUNTIME_LIFECYCLE_REGISTERED_IN_WORKING_TREE',gateId:'f2-productive-acceptance-exact-successor-v20260818',lifecycle:RUNTIME,persistentSourceChanged:false},null,2));
