#!/usr/bin/env node
'use strict';
import fs from 'node:fs';
import path from 'node:path';
import {execFileSync} from 'node:child_process';
const ROOT=process.cwd();
const OUT=path.join(ROOT,'orbit360-platform/runtime-gate-crm-v20260716/preflight-sanitizado.json');
const GATE='block6-go-live-product-v20260730';
const VERSION='6.2.0';
const REQUEST='tools/orbit360-m6-final-closure-request-v20260730.json';
const WORKFLOW='.github/workflows/orbit360-m6-corrective-go-live-v20260730.yml';
const ROOT_CAUSE='tools/orbit360-m6-legal-gate-race-root-cause-v20260730.json';
const HELPER='tools/orbit360-browser-blocking-gate-readiness-v20260730.mjs';
const TEST='tools/orbit360-browser-blocking-gate-readiness-test-v20260730.mjs';
const SMOKE='tools/orbit360-m6-product-browser-smoke-v20260730.mjs';
const POLICY='orbit360-platform/core/tenant-access-policy-product-p0.js';
const APP='orbit360-platform/core/product-app-runtime-p0.js';
const STATIC_RUN=30552591248;
const checks=[];
const add=(id,ok,detail='')=>checks.push({id,ok:Boolean(ok),detail:String(detail||'').slice(0,260)});
const read=rel=>fs.readFileSync(path.join(ROOT,rel),'utf8');
function write(payload){fs.mkdirSync(path.dirname(OUT),{recursive:true});fs.writeFileSync(OUT,JSON.stringify(payload,null,2)+'\n','utf8');}
try{
  const req=JSON.parse(read(REQUEST)),rc=JSON.parse(read(ROOT_CAUSE));
  const parent=execFileSync('git',['rev-parse','HEAD^'],{cwd:ROOT,encoding:'utf8'}).trim();
  add('GATE',process.argv[2]===GATE&&req.gateId===GATE&&req.contractVersion===VERSION);
  add('BRANCH',req.branch==='ays/backend-tenant-lab-v99-20260703'&&process.env.ORBIT360_BRANCH===req.branch);
  add('BINDING',req.recoveryBaseCommit===parent,parent);
  add('AUTHORIZATION',req.explicitRecoveryAuthorization===true&&req.authorizationSource==='user_authorized_m6_final_closure_after_reusable_readiness_20260730'&&req.allowedExecutions===1&&req.authorizationConsumed===false&&req.validatorRootCauseClosed===true);
  add('STATIC_REMEDIATION',req.staticValidatorRemediationRun===STATIC_RUN&&req.smokeValidatorRevision==='20260730.7'&&req.reusableBrowserReadiness===true&&req.blockingGateSyntheticProof===true&&req.boundedHostingReadiness===true);
  add('HISTORY',req.previousRecoveryRun===30551563820&&req.previousRecoveryArtifact===8763037814&&req.previousRecoveryResult==='ROLLED_BACK_SAFE');
  add('ROOT_CAUSE',rc.classification==='VALIDATOR_STALE'&&rc.rootCause==='LEGAL_GATE_DEFERRED_RENDER_RACE'&&rc.rollbackSafe===true&&rc.productionLive===false);
  add('TARGET',req.target?.projectId==='ays-orbit-360-lab'&&req.target?.tenantId==='alianzas-soluciones'&&req.target?.liveUrl==='https://ays-orbit-360-lab.web.app'&&req.createProject===false&&req.createStorageBucket===false);
  add('DATA_CONTRACT',JSON.stringify(req.canonicalMigratedCollections)===JSON.stringify(['clientes','aseguradoras'])&&req.logicalCountryField==='country'&&req.physicalCountryField==='pais'&&req.waitAllActiveCollections===true);
  add('SCOPE',req.secrets===true&&req.firestoreRead===true&&req.firestoreDataWrites===false&&req.runtime===true&&req.browser===true&&req.hostingDeploy===true&&req.firestoreRulesDeploy===true&&req.storageRulesDeploy===false&&req.storageDeferredFailClosed===true&&req.functionsDeploy===false&&req.production===true&&req.rollbackOnFailure===true&&req.main===false&&req.merge===false&&req.polizas===false);
  const required=[WORKFLOW,ROOT_CAUSE,HELPER,TEST,SMOKE,POLICY,APP,'firebase.product-go-live.json','firebase.product-rollback-safe.json','firestore.product-readonly.rules','firestore.product-deny-all.rules','tools/orbit360-hosting-readiness-bounded-v20260730.mjs','tools/orbit360-m6-product-data-snapshot-readonly-v20260730.mjs','tools/orbit360-m6-build-product-shell-v20260730.mjs'];
  add('FILES',required.every(r=>fs.existsSync(path.join(ROOT,r))),required.filter(r=>!fs.existsSync(path.join(ROOT,r))).join(','));
  for(const r of required.filter(x=>/\.(?:js|mjs)$/.test(x)))execFileSync(process.execPath,['--check',r],{cwd:ROOT,stdio:'pipe'});
  add('SYNTAX',true);
  const wf=read(WORKFLOW),smoke=read(SMOKE),helper=read(HELPER),policy=read(POLICY),app=read(APP);
  add('BLOCKING_GATE_HELPER',helper.includes('settleBlockingGates')&&helper.includes('arrivalWindowMs')&&helper.includes('quietWindowMs')&&helper.includes("locator('#lg-chk')")&&helper.includes("locator('#lg-ok')")&&!helper.includes('force:true'));
  const testOut=execFileSync(process.execPath,[TEST],{cwd:ROOT,encoding:'utf8',stdio:['ignore','pipe','pipe']}).trim();
  add('BLOCKING_GATE_SYNTHETIC',/PASS/.test(testOut)&&/520/.test(testOut),testOut);
  add('SMOKE_VALIDATOR',smoke.includes("contractVersion:'6.2.0'")&&smoke.includes("validatorRevision:'20260730.7'")&&smoke.includes('await settleBlockingGates(page')&&smoke.includes("report.checks.blockingGates=true")&&smoke.includes("report.checks.insurerSemanticClick=true"));
  add('QUERY_ALIAS',policy.includes("QUERY_FIELD_ALIASES = Object.freeze({ country: 'pais' })")&&policy.includes('productPhysicalFieldAliasesApplied = true'));
  add('ALL_COLLECTION_BARRIER',app.includes('waitActiveCollections')&&app.includes("expected.every(function(name){return done.indexOf(name)>=0;})"));
  add('ONE_STABLE_WORKFLOW',wf.includes(REQUEST)&&wf.includes('M6 · final closure 6.2.0')&&wf.includes("needs.static_preflight.outputs.recovery_requested == 'true'"));
  add('SEPARATED_STAGES',wf.includes('id: firebase_deploy')&&wf.includes('id: hosting_readiness')&&wf.includes('id: browser')&&wf.includes('id: data_integrity')&&wf.includes('id: rollback'));
  add('STORAGE_DEFERRED',wf.includes('firestore:rules,hosting')&&!wf.includes('firestore:rules,storage,hosting'));
  const failed=checks.filter(c=>!c.ok),profile={secrets:true,firestoreRead:true,writes:false,runtime:true,browser:true,deploy:true,functionsDeploy:false,rulesDeploy:true,production:true};
  const out={schemaVersion:'orbit360-gate-contract-preflight-m6-final-closure-v1',gateId:GATE,contractVersion:VERSION,executionPhase:'M6_PRODUCT_GO_LIVE_RECOVERY_EXECUTION',status:failed.length?'VALIDATOR_STALE':'GO_GATE_CONTRACT',classification:failed.length?'VALIDATOR_STALE':null,total:checks.length,passed:checks.length-failed.length,failed:failed.length,failedCheckIds:failed.map(c=>c.id),checks,executionAuthorized:failed.length===0,allowedExecutions:failed.length?0:1,macroClosure:true,staticValidatorRemediationRun:STATIC_RUN,previousRecoveryRun:30551563820,previousRecoveryArtifact:8763037814,validatorRootCauseClosed:true,reusableBrowserReadiness:true,blockingGateSyntheticProof:true,futureModulesReuse:true,canonicalMigratedCollections:['clientes','aseguradoras'],logicalCountryField:'country',physicalCountryField:'pais',waitAllActiveCollections:true,semanticInsurerCardClick:true,viewportHitTest:true,smokeValidatorRevision:'20260730.7',boundedHostingReadiness:true,storageDeferredFailClosed:true,createStorageBucket:false,storageRulesDeploy:false,capabilityProfile:profile,dataAccess:false,secretAccess:false,firestoreRead:false,firestoreWrites:0,operationalWrites:0,runtimeExecuted:false,browserExecuted:false,rulesApplied:false,deployExecuted:false,productionTouched:false,containsPII:false,containsSecrets:false};
  write(out);console.log(JSON.stringify(out,null,2));if(failed.length)process.exit(41);
}catch(error){write({schemaVersion:'orbit360-gate-contract-preflight-m6-final-closure-v1',gateId:GATE,contractVersion:VERSION,status:'VALIDATOR_STALE',classification:'VALIDATOR_STALE',failed:1,failedCheckIds:['M6_FINAL_CLOSURE_PREFLIGHT_EXCEPTION'],error:String(error&&error.message||error).slice(0,500),executionAuthorized:false,allowedExecutions:0,dataAccess:false,secretAccess:false,firestoreRead:false,firestoreWrites:0,operationalWrites:0,runtimeExecuted:false,browserExecuted:false,rulesApplied:false,deployExecuted:false,productionTouched:false,containsPII:false,containsSecrets:false});process.exit(41);}
