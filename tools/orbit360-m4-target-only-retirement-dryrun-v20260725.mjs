#!/usr/bin/env node
'use strict';
import fs from 'node:fs';import path from 'node:path';import process from 'node:process';import crypto from 'node:crypto';import vm from 'node:vm';
import {applicationDefault,getApps,initializeApp,deleteApp} from 'firebase-admin/app';import {getFirestore} from 'firebase-admin/firestore';
const ROOT=process.cwd(),PROJECT_ID='ays-orbit-360-lab',TENANT_ID='alianzas-soluciones',GATE_ID='block4-target-only-retirement-dryrun-v20260725',CONTRACT_VERSION='4.2.8';
const OUT=path.join(ROOT,'orbit360-platform/runtime-gate-crm-v20260716/m4-target-only-retirement-dryrun-summary.json');const CLOSURE=path.join(ROOT,'orbit360-platform/runtime-gate-crm-v20260716/m4-target-only-reconciliation-closure.json');
const TECH=/\b(demo|test|testing|smoke|placeholder|sample|seed|synthetic|temporal|tmp|orbit\.lab)\b/i;const VOLATILE=new Set(['_syncStatus','_syncOp','_syncError','_syncAt','createdAt','updatedAt','fechaActualizacion','lastModifiedAt']);
function text(v){return String(v==null?'':v).trim();}function plain(v){if(v==null)return v;if(v&&typeof v.toDate==='function')return v.toDate().toISOString();if(Array.isArray(v))return v.map(plain);if(typeof v==='object'){const o={};Object.keys(v).sort().forEach(k=>{if(!VOLATILE.has(k))o[k]=plain(v[k]);});return o;}return v;}function hash(v){return crypto.createHash('sha256').update(typeof v==='string'?v:JSON.stringify(v)).digest('hex');}function norm(v){return text(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();}function first(row,keys){for(const k of keys){if(text(row&&row[k]))return row[k];}return '';}function country(row){const raw=norm(first(row,['country','pais','país','countryCode','country_code'])).replace(/\s+/g,'');if(['gt','gtm','guatemala'].includes(raw))return'GT';if(['co','col','colombia'].includes(raw))return'CO';return'';}
function fingerprints(row,collection){const out=[];function add(kind,values){const clean=values.map(norm);if(clean.every(Boolean))out.push(kind+':'+hash(clean.join('|')));}if(collection==='clientes'){add('document',[first(row,['numeroDocumento','numero_documento','documento','identificacion','dpi','nit','rut','cedula','cedulaJuridica','cedula_juridica'])]);add('email',[first(row,['email','correo','correoElectronico'])]);add('phone',[first(row,['telefono','phone','celular','movil'])]);add('name_country',[first(row,['nombre','razonSocial','razon_social','name','legalName']),country(row)]);}else{add('code',[first(row,['codigo','code','insurerCode','aseguradoraCodigo'])]);add('tax',[first(row,['nit','taxId','tax_id'])]);add('name_country',[first(row,['nombre','razonSocial','razon_social','name','legalName']),country(row)]);}return [...new Set(out)];}
function technicalMarker(row){const parts=[];function walk(v,d){if(d>2||v==null)return;if(Array.isArray(v)){v.slice(0,20).forEach(x=>walk(x,d+1));return;}if(typeof v==='object'){Object.keys(v).slice(0,80).forEach(k=>{parts.push(k);walk(v[k],d+1);});return;}if(typeof v==='string'||typeof v==='number')parts.push(String(v));}walk(row,0);return TECH.test(parts.join(' '));}
function sourceIndex(snapshot,collection){const idx=new Map();snapshot.docs.forEach(doc=>fingerprints(plain(doc.data()||{}),collection).forEach(fp=>idx.set(fp,(idx.get(fp)||0)+1)));return idx;}
function select(sourceSnap,targetSnap,collection,targetPath){const sourceIds=new Set(sourceSnap.docs.map(d=>d.id)),idx=sourceIndex(sourceSnap,collection),items=[];for(const doc of targetSnap.docs){if(sourceIds.has(doc.id))continue;const row=plain(doc.data()||{}),matches=fingerprints(row,collection).reduce((a,fp)=>a+(idx.get(fp)||0),0),tech=technicalMarker(row);if(matches===0&&tech)items.push({collection,id:doc.id,path:targetPath+'/'+doc.id,snapshot:row,technicalMarker:tech,noSourceIdMatch:true,noFingerprintMatch:true});}return items;}
function write(v){fs.mkdirSync(path.dirname(OUT),{recursive:true});fs.writeFileSync(OUT,JSON.stringify(v,null,2)+'\n','utf8');}
async function main(){let app;const base={schemaVersion:'orbit360-m4-target-only-retirement-dryrun-v1',gateId:GATE_ID,contractVersion:CONTRACT_VERSION,projectId:PROJECT_ID,tenantId:TENANT_ID,readOnly:true,writeAuthorized:false,writeExecuted:false,configurationWrites:0,membershipWrites:0,clientWrites:0,insurerWrites:0,auditWrites:0,deletes:0,merges:0,rulesChanged:false,hostingDeploy:false,functionsDeploy:false,imports:false,policies:false,mergeMain:false,containsPII:false,containsSecrets:false,secretValueCount:0,privacyMode:'aggregate_plan_and_digests_only'};
 try{if(text(process.env.ORBIT360_EXISTING_FIREBASE_PROJECT_ID)!==PROJECT_ID)throw new Error('PIPELINE_MECHANISM_FAILURE:EXISTING_PROJECT_IDENTITY_NOT_RESOLVED');const closure=JSON.parse(fs.readFileSync(CLOSURE,'utf8'));if(closure.status!=='M4_TARGET_ONLY_RECONCILIATION_READONLY_CLOSED_SUCCESS'||closure.reconciliation?.classifications?.aggregate?.obsolete!==4||closure.reconciliation?.classifications?.aggregate?.requiresValidation!==0||closure.approvalReadyForTargetOnlyResolutionPlanning!==true)throw new Error('DATA_CONTRACT_FAILURE:PRIOR_RECONCILIATION_CLOSURE_INVALID');globalThis.window=globalThis;globalThis.Orbit={};vm.runInThisContext(fs.readFileSync(path.join(ROOT,'orbit360-platform/core/m4-target-only-retirement-dryrun-contract-p0.js'),'utf8'));app=getApps()[0]||initializeApp({credential:applicationDefault(),projectId:PROJECT_ID});const db=getFirestore(app),legacy=db.collection('tenantId').doc(TENANT_ID),tenant=db.collection('tenants').doc(TENANT_ID);const [sc,tc,si,ti]=await Promise.all([legacy.collection('clientes').get(),tenant.collection('data').doc('clientes').collection('items').get(),legacy.collection('aseguradoras').get(),tenant.collection('data').doc('aseguradoras').collection('items').get()]);const clients=select(sc,tc,'clientes','tenants/'+TENANT_ID+'/data/clientes/items'),insurers=select(si,ti,'aseguradoras','tenants/'+TENANT_ID+'/data/aseguradoras/items'),selected=clients.concat(insurers).sort((a,b)=>(a.collection+'|'+a.id).localeCompare(b.collection+'|'+b.id));if(clients.length!==2||insurers.length!==2||selected.length!==4)throw new Error('DATA_CONTRACT_FAILURE:EXACT_OBSOLETE_SELECTION_CHANGED');const selectionDigest=hash(selected.map(x=>({collection:x.collection,id:x.id,snapshotHash:hash(x.snapshot)}))),snapshotDigest=hash(selected.map(x=>({path:x.path,snapshot:x.snapshot})));const payload={...base,remoteReadConfirmed:true,priorReconciliationClosureValidated:true,sourceCounts:{clientes:sc.size,aseguradoras:si.size},targetCollectionSemantic:'target_only_overlay',targetCollectionCounts:{clientes:tc.size,aseguradoras:ti.size},selection:{clientSelected:clients.length,insurerSelected:insurers.length,totalSelected:selected.length,classification:'obsolete',selectionRule:'target_only_and_technical_marker_and_no_source_equivalence',deterministicOrder:'collection_then_document_id_asc',deterministicSelection:true,selectionDigest,snapshotDigest,allTargetOnly:true,allTechnicalMarker:selected.every(x=>x.technicalMarker),allNoSourceIdMatch:selected.every(x=>x.noSourceIdMatch),allNoFingerprintMatch:selected.every(x=>x.noFingerprintMatch),rawValuesExported:false,recordIdsExported:false,pseudonymousTokensExported:false},diff:{retireCandidates:4,wouldDeleteClients:2,wouldDeleteInsurers:2,actualDeletes:0,actualUpdates:0,actualCreates:0,actualMerges:0},afterProjection:{targetClientsAfterHypotheticalRetirement:tc.size-clients.length,targetInsurersAfterHypotheticalRetirement:ti.size-insurers.length,targetOnlyRemaining:0,approvalReadyForM4Write:false},traceability:{mode:'deterministic_target_only_retirement_plan',sourceCollections:2,targetCollections:2,beforeAfterPlanned:true,recordIdsExported:false},rollback:{mode:'exact_restore_from_four_before_snapshots',planned:true,executed:false,snapshotCountPlanned:4,restorePathCountPlanned:4,restoreOrder:'reverse_deterministic_selection',snapshotValuesExported:false,snapshotDigest},auditPlan:{mode:'append_only',planned:true,executed:false,plannedEvents:4},collectionScope:{collectionsRead:4,sourceClients:'tenantId/{tenant}/clientes',targetClients:'tenants/{tenant}/data/clientes/items',sourceInsurers:'tenantId/{tenant}/aseguradoras',targetInsurers:'tenants/{tenant}/data/aseguradoras/items'}};const checked=globalThis.Orbit.m4TargetOnlyRetirementDryrunP0.build(payload);write({...payload,...checked,ok:checked.ok,status:checked.status,classification:checked.ok?null:'DATA_CONTRACT_FAILURE',containsPII:false,containsSecrets:false});if(!checked.ok)process.exitCode=41;
 }catch(error){const msg=text(error&&error.message||error),classification=msg.startsWith('PIPELINE_MECHANISM_FAILURE')?'PIPELINE_MECHANISM_FAILURE':'DATA_CONTRACT_FAILURE';write({...base,ok:false,status:classification,classification,error:msg.replace(/[A-Za-z0-9_-]{20,}/g,'[redacted]').slice(0,240),remoteReadConfirmed:false,containsPII:false,containsSecrets:false});process.exitCode=41;}finally{if(app)await deleteApp(app).catch(()=>{});}}
await main();
