#!/usr/bin/env node
'use strict';
import fs from 'node:fs';import path from 'node:path';
const ROOT=process.cwd(),GATE_ID=process.argv[2]||'block2-product-readonly-runtime-v20260723',EVIDENCE_REL='orbit360-platform/runtime-gate-crm-v20260716/preflight-sanitizado.json',EVIDENCE_PATH=path.join(ROOT,EVIDENCE_REL);
const files={lifecycle:'tools/orbit360-validator-lifecycle-contract-m2-runtime-v20260723.json',registry:'tools/orbit360-gate-contract-registry-extension-m2-runtime-v20260723.json',overlay:'tools/orbit360-gate-contract-overlay-m2-runtime-v20260723.json',freeze:'tools/orbit360-incident-freeze-v20260721.json',runtimeAuth:'tools/orbit360-m2-existing-identity-runtime-authorization-v20260724.json',staticAuth:'tools/orbit360-m2-membership-root-cause-static-authorization-v20260724.json',readiness:'orbit360-platform/core/backend-product-readiness-contract-p0.js',runtime:'tools/orbit360-m2-existing-identity-runtime-v20260724.mjs',workflow:'.github/workflows/orbit360-m2-existing-identity-runtime-gate-v20260724.yml'};
const read=p=>fs.readFileSync(path.join(ROOT,p),'utf8'),json=p=>JSON.parse(read(p));const checks=[];const check=(id,ok,detail='')=>checks.push({id,ok:!!ok,detail:String(detail)});
try{
 Object.entries(files).forEach(([id,p])=>check('FILE:'+id,fs.existsSync(path.join(ROOT,p)),p));
 const lifecycle=json(files.lifecycle),registry=json(files.registry),overlay=json(files.overlay),freeze=json(files.freeze),runtimeAuth=json(files.runtimeAuth),staticAuth=json(files.staticAuth);
 check('GATE',lifecycle.gateId===GATE_ID&&lifecycle.gateContractVersion==='2.2.1');
 check('PHASE',lifecycle.executionProfile&&lifecycle.executionProfile.phase==='EXISTING_IDENTITY_RUNTIME_EXECUTION');
 const caps=lifecycle.executionProfile&&lifecycle.executionProfile.capabilities||{};
 check('CAPABILITIES_RUNTIME',caps.secrets===true&&caps.firestoreRead===true&&caps.writes===false&&caps.runtime===true&&caps.browser===false&&caps.deploy===false&&caps.functionsDeploy===false&&caps.rulesDeploy===false&&caps.production===false);
 check('RUNTIME_AUTHORIZED',runtimeAuth.status==='AUTHORIZED_ONCE_AFTER_TWO_VALIDATOR_STALE_CLOSURES'&&runtimeAuth.authorization.allowedExecutions===1&&runtimeAuth.authorization.consumed===false,runtimeAuth.status);
 check('RUNTIME_SCOPE',runtimeAuth.authorization.readOnlyRuntime===true&&runtimeAuth.authorization.existingIdentityOnly===true&&runtimeAuth.authorization.createAuthUser===false&&runtimeAuth.authorization.updateAuthUser===false&&runtimeAuth.authorization.createMembership===false&&runtimeAuth.authorization.updateMembership===false&&runtimeAuth.authorization.applyRules===false&&runtimeAuth.authorization.configurationWrites===false&&runtimeAuth.authorization.operationalWrites===false);
 check('STATIC_CLOSED',staticAuth.status==='CONSUMED_PASS'&&staticAuth.authorization.allowedExecutions===0&&staticAuth.result&&staticAuth.result.ok===true,staticAuth.status);
 check('TWO_VALIDATOR_STALE_CLOSED',runtimeAuth.validatorStaleClosures&&runtimeAuth.validatorStaleClosures.auth.closed===true&&runtimeAuth.validatorStaleClosures.membership.closed===true);
 check('FREEZE_AUTHORIZED',freeze.status==='M2_RUNTIME_V3_AUTHORIZED_ONCE'&&freeze.m2RuntimeAuthorization&&freeze.m2RuntimeAuthorization.active===true&&freeze.m2RuntimeAuthorization.allowedExecutions===1,freeze.status);
 check('REGISTRY_AUTHORIZED',registry.planPatch&&registry.planPatch.currentObjective==='RUNTIME_V3_AUTHORIZED_ONCE_REQUEST_PENDING');
 check('OVERLAY_AUTHORIZED',overlay.gatePatch&&overlay.gatePatch.status==='M2_RUNTIME_V3_AUTHORIZED_ONCE'&&overlay.effectiveOwnerReconciliation&&overlay.effectiveOwnerReconciliation.allowedRuntimeExecutions===1);
 const readiness=read(files.readiness),runtime=read(files.runtime),workflow=read(files.workflow);
 check('READINESS_AUTH_MEMBERSHIP_GUARDS',readiness.includes('controlledAuthMarkerAccepted')&&readiness.includes('controlledMembershipMarkerAccepted')&&readiness.includes('controlledIdentityBindsAuthAndMembership'));
 check('READINESS_GENERIC',!readiness.includes('alianzas-soluciones'));
 check('RUNTIME_EVIDENCE',runtime.includes('SAFE_CODE_PREFIXES')&&runtime.includes('extractReadinessErrors')&&runtime.includes('controlledAuthMarkerAccepted')&&runtime.includes('controlledMembershipMarkerAccepted'));
 check('WORKFLOW_V3',workflow.includes('runtime-request-v20260724-v3.json')&&workflow.includes("orbit360-m2-existing-identity-runtime-request-v3")&&workflow.includes('orbit360/m2-existing-identity-runtime-v3'));
 check('WORKFLOW_ACCEPTANCE',workflow.includes('.controlledAuthMarkerAccepted==true')&&workflow.includes('.controlledMembershipMarkerAccepted==true')&&workflow.includes('.storeInstalled==true')&&workflow.includes('.snapshotsAttached==true')&&workflow.includes('.localWriteBlocked==true'));
 check('WORKFLOW_NO_WRITES',!workflow.includes('firebase deploy')&&!workflow.includes('applyRules')&&!workflow.includes('createUser(')&&!workflow.includes('setDoc('));
}catch(error){check('ENGINE_EXECUTION',false,error&&error.message||error);}
const failed=checks.filter(x=>!x.ok),payload={schemaVersion:'orbit360-gate-contract-preflight-m2-runtime-v3-authorized-v1',gateId:GATE_ID,contractVersion:'2.2.1',executionPhase:'EXISTING_IDENTITY_RUNTIME_EXECUTION',generatedAt:new Date().toISOString(),status:failed.length?'VALIDATOR_STALE':'GO_GATE_CONTRACT',classification:failed.length?'VALIDATOR_STALE':null,total:checks.length,passed:checks.length-failed.length,failed:failed.length,failedCheckIds:failed.map(x=>x.id),checks,twoValidatorStaleClosuresProven:failed.length===0,runtimeAuthorized:failed.length===0,allowedRuntimeExecutions:failed.length?0:1,sourceTransformed:false,dataAccess:false,secretAccess:false,firestoreRead:false,operationalWrites:0,controlledConfigurationWritesExecuted:0,runtimeExecuted:false,browserExecuted:false,rulesApplied:false,deployExecuted:false,productionTouched:false,containsPII:false,containsSecrets:false};
fs.mkdirSync(path.dirname(EVIDENCE_PATH),{recursive:true});fs.writeFileSync(EVIDENCE_PATH,JSON.stringify(payload,null,2)+'\n');console.log(JSON.stringify(payload,null,2));process.exit(failed.length?41:0);
