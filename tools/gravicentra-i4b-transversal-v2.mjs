import fs from 'node:fs';
import path from 'node:path';
import process from 'node:process';
import { chromium } from 'playwright';
import { initializeApp, cert, deleteApp } from 'firebase-admin/app';
import { getAuth } from 'firebase-admin/auth';
import { getFirestore } from 'firebase-admin/firestore';

const PROJECT='ays-orbit-360-lab', TENANT='alianzas-soluciones';
const PREVIEW=String(process.env.PREVIEW_URL||'').replace(/\/$/,''), SOURCE=String(process.env.SOURCE_SHA||''), BUILD=String(process.env.BUILD_ID||'');
const OUT=process.env.I4B_EVIDENCE_DIR||process.env.RUNNER_TEMP||process.cwd();
const ROLES=['Dirección','SuperAdmin','AdminTenant','Operativo','Asesor'];
const PRIV=new Set(['Dirección','SuperAdmin','AdminTenant','Operativo']);
const VIEWPORTS=[{id:'desktop',width:1440,height:1000},{id:'tablet',width:1024,height:768},{id:'mobile',width:390,height:844}];
const ROUTES=[['inicio','#/inicio'],['cliente360','#/cliente360'],['aseguradoras','#/aseguradoras'],['ops','#/ops'],['leads','#/leads'],['polizas','#/polizas'],['cobros','#/cobros']];
const CANONICAL_STAGES=new Set(['nuevo','contactado','cotizando','propuesta','negociacion','inspeccion','emision','emitido','perdido']);
const OPS_STAGES=new Set(['cotizando','inspeccion','emision']);
const clean=v=>String(v==null?'':v).trim();
const need=(ok,code)=>{if(!ok)throw new Error(code)};
need(/^[0-9a-f]{40}$/.test(SOURCE),'I4B_SOURCE_INVALID');
need(/^gi-i3-[0-9a-f]{12}-[0-9a-f]{12}$/.test(BUILD)&&BUILD.includes(SOURCE.slice(0,12)),'I4B_BUILD_INVALID');
need(/^https:\/\/[A-Za-z0-9._-]+\.web\.app$/.test(PREVIEW),'I4B_PREVIEW_INVALID');

function normRole(v){const k=clean(v).toLowerCase().replace(/\s+/g,' ');return ({'dirección':'Dirección','direccion':'Dirección','director':'Dirección','superadmin':'SuperAdmin','super admin':'SuperAdmin','super_admin':'SuperAdmin','super-admin':'SuperAdmin','admin':'AdminTenant','administrador':'AdminTenant','admin tenant':'AdminTenant','admin_tenant':'AdminTenant','admintenant':'AdminTenant','operativo':'Operativo','operaciones':'Operativo','asesor':'Asesor'})[k]||clean(v);}
function rolesOf(m){const x=Array.isArray(m?.roles)?m.roles:Array.isArray(m?.rolesAsignados)?m.rolesAsignados:(m?.role||m?.rol?[m.role||m.rol]:[]);return [...new Set(x.map(normRole).filter(Boolean))];}
function activeRole(m,rs){return normRole(m?.activeRole||m?.rolActivo||m?.defaultRole||m?.rolDefault||m?.roleDefault||rs[0]);}
function serviceAccount(){for(const raw of [process.env.SA_DEFAULT,process.env.SA_ORBIT360_LAB,process.env.SA_ORBIT_360_LAB].filter(Boolean)){try{const x=JSON.parse(raw);if(x?.type==='service_account'&&x?.project_id===PROJECT&&x?.client_email&&x?.private_key)return x;}catch{}}throw new Error('I4B_SERVICE_ACCOUNT_UNAVAILABLE');}
function telemetry(page){const t={console:[],page:[],req:[],http:[]};page.on('console',m=>{if(m.type()==='error')t.console.push(m.text().slice(0,240));});page.on('pageerror',e=>t.page.push(String(e?.message||e).slice(0,240)));page.on('requestfailed',q=>{try{if(new URL(q.url()).origin===new URL(PREVIEW).origin)t.req.push(q.url());}catch{}});page.on('response',q=>{try{if(new URL(q.url()).origin===new URL(PREVIEW).origin&&q.status()>=400)t.http.push({status:q.status(),url:q.url()});}catch{}});return t;}
function addFail(rec,code,detail){rec.failures.push(detail?`${code}:${detail}`:code);}
async function check(rec,code,fn){try{const value=await fn();rec.checks.push({code,pass:true});return value;}catch(e){addFail(rec,code,String(e?.message||e));rec.checks.push({code,pass:false});return null;}}

async function activate(page,token){const started=await page.evaluate(async tok=>{const p=Orbit.productRuntimeBrowserProvidersP0,c=await p.initialize();await c.modules.auth.signInWithCustomToken(c.auth,tok);return Orbit.productAppP0.activate();},token);need(started?.started===true,'APP_NOT_STARTED');await page.waitForFunction(()=>Orbit?.productAppP0?.status?.().started===true&&!document.body.classList.contains('pre-auth'),null,{timeout:12000});}
async function acceptLegal(page){const gate=page.locator('[data-legal-gate].open');try{await gate.waitFor({state:'visible',timeout:2000});}catch{return;}const chk=gate.locator('#lg-chk'),ok=gate.locator('#lg-ok');if(await chk.count()===1&&await ok.count()===1){await chk.check();await ok.click();await gate.waitFor({state:'detached',timeout:4000});}}
async function setRole(page,target){const before=await page.evaluate(()=>({active:Orbit?.session?.rol?.()||'',assigned:Orbit?.session?.allowedRoles?.()||[]}));if(before.active!==target){need(before.assigned.includes(target),'ROLE_NOT_ASSIGNED');need(await page.evaluate(r=>Orbit.session.set(r),target),'ROLE_SWITCH_REJECTED');await page.waitForTimeout(180);}need(await page.evaluate(()=>Orbit?.session?.rol?.()||'')===target,'ROLE_SWITCH_NOT_EFFECTIVE');}
async function routeProbe(page,key,hash){const t=Date.now();await page.evaluate(h=>{location.hash=h;},hash);await page.waitForFunction(k=>Orbit?.route?.key===k&&!!document.querySelector('#host'),key,{timeout:12000});await page.waitForTimeout(150);const s=await page.evaluate(()=>{const host=document.querySelector('#host'),root=document.documentElement,body=document.body,text=(host?.textContent||'').replace(/\s+/g,' ').trim();return{routeKey:Orbit?.route?.key||'',hostTextLength:text.length,horizontalOverflowPx:Math.max(0,(root?.scrollWidth||0)-innerWidth,(body?.scrollWidth||0)-innerWidth)};});need(s.routeKey===key,'ROUTE_KEY_MISMATCH');need(s.hostTextLength>10,'ROUTE_EMPTY');need(s.horizontalOverflowPx<=24,'HORIZONTAL_OVERFLOW_'+s.horizontalOverflowPx);return{...s,routeMs:Date.now()-t};}
async function waitReadModels(page){await page.waitForFunction(()=>{const s=Orbit?.store?._productStatus?.()||{},c=new Set(s.serverConfirmedCollections||[]);return ['recibosEsperados','carteraPrimas','negocios'].every(x=>c.has(x));},null,{timeout:14000});return page.evaluate(()=>{const s=Orbit?.store?._productStatus?.()||{},count=n=>(Orbit.store.all(n)||[]).length;return{serverConfirmedCollections:[...(s.serverConfirmedCollections||[])],clientes:count('clientes'),polizas:count('polizas'),vehiculos:count('vehiculos'),recibosEsperados:count('recibosEsperados'),carteraPrimas:count('carteraPrimas'),cobros:count('cobros'),negocios:count('negocios')};});}
async function relationshipProbe(page){return page.evaluate(()=>{const all=n=>{try{return Orbit.store.all(n)||[]}catch{return[]}},sc=(m,n)=>{try{return Orbit.access?.scopedStore?.(m)?.all?.(n)||[]}catch{return[]}};const clientes=all('clientes'),polizas=all('polizas'),vehiculos=all('vehiculos'),cids=new Set(clientes.map(x=>String(x.id))),pids=new Set(polizas.map(x=>String(x.id)));const isLab=x=>/^lab_/i.test(String(x?.id||''))||/^lab_/i.test(String(x?.clienteId||''));const orphanPol=polizas.filter(x=>x?.clienteId&&!cids.has(String(x.clienteId)));const orphanVehCli=vehiculos.filter(x=>x?.clienteId&&!cids.has(String(x.clienteId)));const orphanVehPol=vehiculos.filter(x=>x?.polizaId&&!pids.has(String(x.polizaId)));const safe=x=>({id:String(x?.id||''),clienteId:String(x?.clienteId||''),polizaId:String(x?.polizaId||''),numero:String(x?.numero||x?.numeroPoliza||''),etapa:String(x?.etapa||''),pais:String(x?.pais||''),origen:String(x?.origen||''),archivado:x?.archivado===true,colLeads:String(x?.colLeads||'')});const opsBoard=(Orbit.ciclo?.opsBoard?.()||[]).flatMap(c=>(c.items||[]).filter(i=>i?.kind==='negocio').map(i=>String(i.rec?.id||''))).filter(Boolean);const leadsBoard=(Orbit.ciclo?.leadsBoard?.()||[]).flatMap(c=>(c.items||[]).filter(i=>i?.kind==='negocio').map(i=>String(i.rec?.id||''))).filter(Boolean);return{totals:{clientes:clientes.length,polizas:polizas.length,vehiculos:vehiculos.length,negocios:all('negocios').length},orphans:{policyClient:orphanPol.map(safe),vehicleClient:orphanVehCli.map(safe),vehiclePolicy:orphanVehPol.map(safe),operationalPolicyClient:orphanPol.filter(x=>!isLab(x)).map(safe),operationalVehicleClient:orphanVehCli.filter(x=>!isLab(x)).map(safe),operationalVehiclePolicy:orphanVehPol.filter(x=>!isLab(x)).map(safe),labPolicies:orphanPol.filter(isLab).map(safe),labVehicles:orphanVehCli.filter(isLab).map(safe)},workflow:{raw:all('negocios').map(safe),scopedLeads:sc('leads','negocios').map(safe),scopedOps:sc('ops','negocios').map(safe),leadsBoardIds:leadsBoard,opsBoardIds:opsBoard}};});}
async function labPolicyUiProbe(page,number){await page.evaluate(()=>{location.hash='#/polizas';});await page.waitForFunction(()=>Orbit?.route?.key==='polizas'&&!!document.querySelector('#host .page'),null,{timeout:12000});const input=page.locator('#fq');if(await input.count()!==1)return{searchAvailable:false,visible:false};await input.fill(number);await input.dispatchEvent('input');await page.waitForTimeout(250);const text=(await page.locator('#host').innerText()).replace(/\s+/g,' ');return{searchAvailable:true,visible:text.includes(number)};}
async function insurerProbe(page,target){await page.evaluate(()=>{location.hash='#/aseguradoras';});await page.waitForFunction(()=>Orbit?.route?.key==='aseguradoras'&&!!document.querySelector('#host .page'),null,{timeout:12000});const x=await page.evaluate(()=>{const rows=Orbit.store.all('aseguradoras')||[],a=rows.find(r=>r&&Array.isArray(r.portales)&&r.portales.some(p=>p&&(p.credentialRef||p.password||p.pass||p.contrasena||p.clave)));return a?{id:a.id,refCount:a.portales.filter(p=>p&&p.credentialRef).length}:null;});need(x?.id,'CREDENTIAL_DATASET_UNAVAILABLE');await page.evaluate(id=>{location.hash='#/aseguradoras?ficha='+encodeURIComponent(id);},x.id);await page.waitForFunction(id=>String(Orbit?.route?.params?.ficha||'')===String(id)&&!!document.querySelector('#asg-ficha'),x.id,{timeout:12000});await page.evaluate(()=>{const el=document.querySelector('#asg-ficha [data-tab="plataformas"]');if(!el)throw new Error('PLATFORM_TAB_MISSING');el.click();});await page.waitForFunction(()=>!!document.querySelector('#af-portales'),null,{timeout:8000});const s=await page.evaluate(()=>({cards:document.querySelectorAll('#af-portales .od-operational-portal-card').length,reveals:document.querySelectorAll('#af-portales [data-od-credential-reveal]').length,copies:document.querySelectorAll('#af-portales [data-od-credential-copy]').length,unavailable:[...document.querySelectorAll('#af-portales .od-credential-box')].filter(x=>/contraseña no disponible|pendiente de conexión segura/i.test(x.textContent||'')).length}));if(PRIV.has(target)){need(s.cards>0,'PRIV_DIRECTORY_EMPTY');if(x.refCount>0){need(s.reveals>0,'PRIV_REVEAL_MISSING');need(s.copies>0,'PRIV_COPY_MISSING');need(s.unavailable===0,'PRIV_MARKED_UNAVAILABLE');}}else{need(s.reveals===0,'ADVISOR_REVEAL_EXPOSED');need(s.copies===0,'ADVISOR_COPY_EXPOSED');}return{...s,refCount:x.refCount,secretRevealed:false};}

const app=initializeApp({credential:cert(serviceAccount()),projectId:PROJECT},'gravicentra-i4b-v2');const auth=getAuth(app),db=getFirestore(app);const members=await db.collection('tenants').doc(TENANT).collection('members').get(),listed=await auth.listUsers(1000),users=new Map(listed.users.map(u=>[u.uid,u])),pool=[];for(const d of members.docs){const m=d.data()||{},uid=clean(m.uid||d.id),u=users.get(uid);if(!u||u.disabled||u.emailVerified!==true||!['active','activo'].includes(clean(m.status||m.estado).toLowerCase()))continue;const rs=rolesOf(m);pool.push({uid,roles:rs,active:activeRole(m,rs)});}const selected=new Map();for(const r of ROLES){const x=pool.find(v=>v.active===r&&v.roles.includes(r))||pool.find(v=>v.roles.includes(r));if(x)selected.set(r,x);}need(selected.size===ROLES.length,'I4B_REQUIRED_ROLE_MEMBERSHIP_MISSING');
const ev={schemaVersion:'gravicentra-i4b-transversal-matrix-v2',gate:'I4B',status:'I4B_MATRIX_FAIL',sourceSha:SOURCE,buildId:BUILD,previewUrl:PREVIEW,expectedContextCount:15,contextsPass:0,contextsFail:0,productionTouched:false,dataTouched:false,writesExecuted:0,failAtEnd:true,cases:[],uniqueFailures:[]};let browser;
try{browser=await chromium.launch({headless:true});for(const role of ROLES){for(const vp of VIEWPORTS){const rec={role,viewport:vp.id,pass:false,failures:[],checks:[],routes:{}};let ctx;try{const token=await auth.createCustomToken(selected.get(role).uid,{gravicentraI4BReadOnly:true});ctx=await browser.newContext({viewport:{width:vp.width,height:vp.height}});const page=await ctx.newPage();page.setDefaultTimeout(12000);const tel=telemetry(page);await check(rec,'LOAD',async()=>{await page.goto(PREVIEW,{waitUntil:'domcontentloaded',timeout:20000});await page.waitForFunction(()=>!!Orbit?.productAppP0&&!!Orbit?.productRuntimeBrowserProvidersP0,null,{timeout:6000});return true;});await check(rec,'ACTIVATE',()=>activate(page,token));await check(rec,'LEGAL',()=>acceptLegal(page));await check(rec,'ROLE',()=>setRole(page,role));for(const [key,hash] of ROUTES){const r=await check(rec,'ROUTE_'+key.toUpperCase(),()=>routeProbe(page,key,hash));if(r)rec.routes[key]=r;}rec.readModels=await check(rec,'READ_MODELS',()=>waitReadModels(page));if(rec.readModels){if(rec.readModels.recibosEsperados<=0)addFail(rec,'I4B_RECIBOS_ESPERADOS_EMPTY');if(rec.readModels.carteraPrimas<=0)addFail(rec,'I4B_CARTERA_PRIMAS_EMPTY');if(rec.readModels.vehiculos<=0)addFail(rec,'I4B_VEHICULOS_EMPTY');}
rec.relationships=await check(rec,'RELATIONSHIP_PROBE',()=>relationshipProbe(page));if(rec.relationships){const o=rec.relationships.orphans,w=rec.relationships.workflow;if(o.operationalPolicyClient.length)addFail(rec,'I4B_OPERATIONAL_CLIENTE_POLIZA_ORPHANS',String(o.operationalPolicyClient.length));if(o.operationalVehicleClient.length)addFail(rec,'I4B_OPERATIONAL_CLIENTE_VEHICULO_ORPHANS',String(o.operationalVehicleClient.length));if(o.operationalVehiclePolicy.length)addFail(rec,'I4B_OPERATIONAL_POLIZA_VEHICULO_ORPHANS',String(o.operationalVehiclePolicy.length));if(o.labPolicies.length||o.labVehicles.length)addFail(rec,'I4B_LAB_RESIDUALS_IN_PRODUCT_STORE',`${o.labPolicies.length}/${o.labVehicles.length}`);for(const n of w.raw){if(!CANONICAL_STAGES.has(n.etapa))addFail(rec,'I4B_NEGOCIO_NONCANONICAL_STAGE',n.id+':'+n.etapa);}const lset=new Set(w.leadsBoardIds),oset=new Set(w.opsBoardIds);for(const n of w.scopedLeads){if(CANONICAL_STAGES.has(n.etapa)&&!lset.has(n.id))addFail(rec,'I4B_LEADS_PROJECTION_MISSING',n.id+':'+n.etapa);}for(const n of w.scopedOps){if(!CANONICAL_STAGES.has(n.etapa))continue;const expected=OPS_STAGES.has(n.etapa);if(expected&&!oset.has(n.id))addFail(rec,'I4B_OPS_PROJECTION_MISSING',n.id+':'+n.etapa);if(!expected&&oset.has(n.id))addFail(rec,'I4B_OPS_PROJECTION_UNEXPECTED',n.id+':'+n.etapa);}}
if(role==='Dirección'&&vp.id==='desktop'&&rec.relationships?.orphans?.labPolicies?.length){rec.labUi=[];for(const p of rec.relationships.orphans.labPolicies){if(p.numero)rec.labUi.push({id:p.id,numero:p.numero,...await check(rec,'LAB_UI_'+p.id,()=>labPolicyUiProbe(page,p.numero))});}for(const x of rec.labUi||[])if(x?.visible)addFail(rec,'I4B_LAB_POLICY_VISIBLE_IN_UI',x.id);}
rec.aseguradoras=await check(rec,'ASEGURADORAS_ROLE',()=>insurerProbe(page,role));rec.telemetry={consoleErrorCount:tel.console.length,pageErrorCount:tel.page.length,sameOriginRequestFailureCount:tel.req.length,sameOriginHttpErrorCount:tel.http.length};if(tel.console.length)addFail(rec,'I4B_CONSOLE_ERRORS',String(tel.console.length));if(tel.page.length)addFail(rec,'I4B_PAGE_ERRORS',String(tel.page.length));if(tel.req.length)addFail(rec,'I4B_REQUEST_FAILURES',String(tel.req.length));if(tel.http.length)addFail(rec,'I4B_HTTP_ERRORS',String(tel.http.length));rec.pass=rec.failures.length===0;if(rec.pass)ev.contextsPass++;else ev.contextsFail++;}catch(e){addFail(rec,'I4B_CONTEXT_FATAL',String(e?.message||e));ev.contextsFail++;}finally{if(ctx)await ctx.close().catch(()=>{});ev.cases.push(rec);}}}
ev.uniqueFailures=[...new Set(ev.cases.flatMap(c=>c.failures.map(x=>x.split(':')[0])))];if(ev.contextsPass===ev.expectedContextCount&&ev.contextsFail===0)ev.status='I4B_MATRIX_PASS';else process.exitCode=1;}finally{if(browser)await browser.close().catch(()=>{});await deleteApp(app).catch(()=>{});fs.mkdirSync(OUT,{recursive:true});fs.writeFileSync(path.join(OUT,'i4b-transversal-matrix-v2.json'),JSON.stringify(ev,null,2)+'\n');console.log('I4B_MATRIX_STATUS='+ev.status);console.log('I4B_CONTEXTS_PASS='+ev.contextsPass+'/'+ev.expectedContextCount);console.log('I4B_CONTEXTS_FAIL='+ev.contextsFail);console.log('I4B_UNIQUE_FAILURES='+ev.uniqueFailures.join(','));console.log('I4B_FAIL_AT_END=true');}
