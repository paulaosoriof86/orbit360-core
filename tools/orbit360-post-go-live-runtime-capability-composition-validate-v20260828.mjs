#!/usr/bin/env node
'use strict';
import fs from 'node:fs';
import path from 'node:path';
import {spawnSync} from 'node:child_process';

const ROOT=process.env.ORBIT360_ROOT?path.resolve(process.env.ORBIT360_ROOT):process.cwd();
const LEDGER='orbit360-platform/docs/orbit360-continuity-ledger-v20260820.json';
const VALIDATOR='tools/orbit360-validar-runtime-capability-composition-v20260828.mjs';
const ASEG='tools/orbit360-aseguradoras-operational-owner-preservation-v20260827.mjs';
const PRES='tools/orbit360-certified-product-preservation-v20260827.mjs';
const TRANSITION='POST_GO_LIVE_RUNTIME_CAPABILITY_COMPOSITION_VALIDATE_SOURCE_ONLY';
const args=process.argv.slice(2);
const val=f=>{const i=args.indexOf(f);return i>=0?String(args[i+1]||''):'';};
const intentPath=val('--intent')||process.env.ORBIT360_EXECUTION_INTENT||'';
const terminalOut=val('--terminal-out')||process.env.ORBIT360_TERMINAL_EVIDENCE||'';
const runId=Number(val('--run-id')||process.env.ORBIT360_EXECUTION_RUN_ID||0);
const selftest=args.includes('--source-only-selftest');
const A=p=>path.join(ROOT,p);
const readJson=p=>JSON.parse(fs.readFileSync(p,'utf8').replace(/^\uFEFF/,''));
const ensure=p=>fs.mkdirSync(path.dirname(p),{recursive:true});
const write=(p,x)=>{if(!p)return;ensure(p);fs.writeFileSync(p,JSON.stringify(x,null,2)+'\n','utf8');};
const zero=extra=>({privilegedRiskObserved:false,secretAccess:false,firestoreRead:false,resetLinksGenerated:0,firestoreWrites:0,authWrites:0,operationalWrites:0,deployExecuted:false,productionTouched:false,runtimeExecuted:false,browserExecuted:false,productMutation:false,dataMutation:false,containsPII:false,containsSecrets:false,...extra});
function fail(code,meta={}){const out=zero({schemaVersion:'orbit360-runtime-capability-composition-validation-terminal-v1',transitionId:TRANSITION,runId:Number.isInteger(runId)?runId:0,ok:false,status:'POST_GO_LIVE_RUNTIME_CAPABILITY_COMPOSITION_VALIDATION_FAIL',classification:'PIPELINE_MECHANISM_FAILURE',failureCode:code,evidencePath:runId>0?`actions-artifact:orbit360-single-state-${runId}/orbit360-terminal.json`:'',...meta});write(terminalOut,out);console.error(JSON.stringify({ok:false,status:out.status,classification:out.classification,code,containsPII:false,containsSecrets:false}));process.exit(41);}
function zeroScope(scope={}){for(const k of ['runtime','browser','secrets','firestoreRead','deploy','production','firestoreWrites','authWrites','operationalWrites','dataWrites','main','merge'])if(scope[k]!==false)throw new Error(`RUNTIME_COMPOSITION_VALIDATE_SCOPE_NOT_ZERO:${k}`);}
function runJson(script,argv=[]){const r=spawnSync(process.execPath,[A(script),...argv],{cwd:ROOT,encoding:'utf8',env:{...process.env,ORBIT360_ROOT:ROOT}});let out={};try{out=JSON.parse(String(r.stdout||'').trim());}catch{throw new Error(`RUNTIME_COMPOSITION_VALIDATE_OUTPUT_INVALID:${script}`);}if(r.status!==0||out.ok!==true)throw new Error(`RUNTIME_COMPOSITION_VALIDATE_DEPENDENCY_FAIL:${script}:${out.status||out.code||r.status}`);return out;}
function validateCurrent(){const p=runJson(PRES);if(p.status!=='CERTIFIED_PRODUCT_BASELINE_PRESERVATION_PASS'||Number(p.changedProductFilesSinceBaseline)!==0)throw new Error('RUNTIME_COMPOSITION_VALIDATE_BASELINE_NOT_PRESERVED');const c=runJson(VALIDATOR,[A('orbit360-platform')]);if(c.status!=='RUNTIME_CAPABILITY_COMPOSITION_PASS'||c.classification!=='PASS')throw new Error('RUNTIME_COMPOSITION_VALIDATE_NOT_PASS');const a=runJson(ASEG);if(a.status!=='ASEGURADORAS_FINAL_OWNER_PRESERVATION_PASS')throw new Error('RUNTIME_COMPOSITION_VALIDATE_ASEG_OWNER_NOT_PASS');return {p,c,a};}
function inspectIntent(I,L){if(I.schemaVersion!=='orbit360-execution-intent-v1'||I.transitionId!==TRANSITION)throw new Error('RUNTIME_COMPOSITION_VALIDATE_INTENT_INVALID');zeroScope(I.scope||{});if(!Number.isInteger(Number(I.acceptanceRunId))||Number(I.acceptanceRunId)<=0)throw new Error('RUNTIME_COMPOSITION_VALIDATE_ACCEPTANCE_RUN_INVALID');if(!/^[A-Za-z0-9._-]{1,120}$/.test(String(I.candidateId||'')))throw new Error('RUNTIME_COMPOSITION_VALIDATE_CANDIDATE_INVALID');for(const k of ['candidateManifestSha256','patchManifestSha256'])if(!/^[a-f0-9]{64}$/.test(String(I[k]||'')))throw new Error(`RUNTIME_COMPOSITION_VALIDATE_DIGEST_INVALID:${k}`);const S=L.postGoLiveSuccessorAcceptance||{},H=L.history?.latestExecutionTerminal||{};if(S.status!=='ACCEPTED_SOURCE_ONLY_PENDING_COMPOSITION_VALIDATION'||S.classification!=='PASS'||Number(S.acceptanceRunId)!==Number(I.acceptanceRunId)||S.candidateId!==I.candidateId||S.candidateManifestSha256!==I.candidateManifestSha256||S.patchManifestSha256!==I.patchManifestSha256)throw new Error('RUNTIME_COMPOSITION_VALIDATE_ACCEPTANCE_BINDING_MISMATCH');if(H.transitionId!=='POST_GO_LIVE_PRODUCT_SUCCESSOR_ACCEPT_SOURCE_ONLY'||H.ok!==true||H.classification!=='PASS'||Number(H.runId)!==Number(I.acceptanceRunId))throw new Error('RUNTIME_COMPOSITION_VALIDATE_ACCEPTANCE_TERMINAL_NOT_PASS');}
if(selftest){try{const {c,a}=validateCurrent();console.log(JSON.stringify(zero({ok:true,status:'POST_GO_LIVE_RUNTIME_CAPABILITY_COMPOSITION_VALIDATION_SELFTEST_PASS',classification:'PASS',sourceOnly:true,capabilityCount:Array.isArray(c.capabilities)?c.capabilities.length:0,exactAcceptanceBindingRequired:true,certifiedBaselinePreserved:true,aseguradorasValidatorSemanticPass:a.ok===true&&a.status==='ASEGURADORAS_FINAL_OWNER_PRESERVATION_PASS',aseguradorasClassificationFieldRequired:false}),null,2));process.exit(0);}catch(error){fail(String(error?.message||error));}}
try{if(!intentPath||!terminalOut||!Number.isInteger(runId)||runId<=0||!fs.existsSync(intentPath))throw new Error('RUNTIME_COMPOSITION_VALIDATE_HANDLER_ARGS_INVALID');const I=readJson(intentPath),L=readJson(A(LEDGER));inspectIntent(I,L);const {c,a}=validateCurrent();const terminal=zero({schemaVersion:'orbit360-runtime-capability-composition-validation-terminal-v1',transitionId:TRANSITION,runId,ok:true,status:'POST_GO_LIVE_RUNTIME_CAPABILITY_COMPOSITION_VALIDATION_PASS',classification:'PASS',acceptanceRunId:Number(I.acceptanceRunId),candidateId:I.candidateId,candidateManifestSha256:I.candidateManifestSha256,patchManifestSha256:I.patchManifestSha256,capabilityCount:Array.isArray(c.capabilities)?c.capabilities.length:0,aseguradorasFinalOwnerAligned:c.aseguradorasFinalOwnerAligned===true,aseguradorasOwnerPreservationStatus:a.status,certifiedNonEntrypointAssetCount:Array.isArray(c.certifiedNonEntrypointAssets)?c.certifiedNonEntrypointAssets.length:0,certifiedBaselinePreserved:true,sourceOnly:true,evidencePath:`actions-artifact:orbit360-single-state-${runId}/orbit360-terminal.json`});write(terminalOut,terminal);console.log(JSON.stringify({ok:true,status:terminal.status,classification:'PASS',candidateId:I.candidateId,acceptanceRunId:Number(I.acceptanceRunId),capabilityCount:terminal.capabilityCount,aseguradorasFinalOwnerAligned:terminal.aseguradorasFinalOwnerAligned,aseguradorasOwnerPreservationStatus:a.status,certifiedBaselinePreserved:true,runtimeExecuted:false,browserExecuted:false,secretAccess:false,firestoreRead:false,deployExecuted:false,productionTouched:false,containsPII:false,containsSecrets:false},null,2));}catch(error){fail(String(error?.message||error));}
