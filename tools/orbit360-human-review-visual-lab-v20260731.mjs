#!/usr/bin/env node
'use strict';
import fs from 'node:fs';
import path from 'node:path';
import {chromium} from 'playwright';
import {settleBlockingGates} from './orbit360-browser-blocking-gate-readiness-v20260730.mjs';
import {TECHNICAL_COPY_PATTERN} from './orbit360-visible-technical-copy-predicate-v20260729.mjs';

const ROOT=process.cwd();
const OUT=path.join(ROOT,'orbit360-platform/runtime-gate-crm-v20260716/human-review-visual-lab-v20260731.json');
const url=String(process.env.ORBIT360_LAB_URL||'').trim();
const email=String(process.env.ORBIT360_LAB_LOGIN_EMAIL||'').trim();
const password=String(process.env.ORBIT360_LAB_LOGIN_PASSWORD||'');
const EXPECT={clientes:430,aseguradoras:30,asesores:7,polizas:1373,vehiculos:1032,recibosEsperados:1293,carteraPrimas:673,cobros:0,finmovs:0};
const report={schemaVersion:'orbit360-human-review-visual-lab-v7',generatedAt:new Date().toISOString(),stage:'init',checks:{},metrics:{},counts:{},readOnly:true,firestoreWrites:0,operationalWrites:0,productionTouched:false,containsPII:false,containsSecrets:false,pageErrors:[]};
const clean=v=>String(v==null?'':v).replace(/https?:\/\/[^/\s]+/g,'[url]').replace(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi,'[email]').replace(/[A-Za-z0-9_-]{24,}/g,'[redacted]').replace(/\s+/g,' ').trim().slice(0,360);
const save=()=>{fs.mkdirSync(path.dirname(OUT),{recursive:true});fs.writeFileSync(OUT,JSON.stringify(report,null,2)+'\n');};
const need=(ok,code,detail='')=>{if(ok)return;const e=new Error(code+(detail?':'+detail:''));e.orbitCode=code;throw e;};
async function route(page,hash,wait=0){const key=String(hash||'').replace(/^#\/?/,'').split('?')[0]||'inicio';await page.evaluate(h=>{if(location.hash===h)window.dispatchEvent(new HashChangeEvent('hashchange'));else location.hash=h;},hash);await page.waitForFunction(expected=>window.Orbit&&Orbit.route&&Orbit.route.key===expected,key,{timeout:15000,polling:50});if(wait)await page.waitForTimeout(wait);}
async function technicalCopy(page){return page.evaluate(pattern=>new RegExp(pattern,'i').test(document.body.innerText||''),TECHNICAL_COPY_PATTERN);}
async function bodyText(page,selector='#host'){return page.locator(selector).innerText();}
async function visibleSemanticLabels(page,rootSelector,labels){return page.evaluate(({rootSelector,labels})=>{const root=document.querySelector(rootSelector);if(!root)return{ok:false,missing:labels,labels:[]};const elements=Array.from(root.querySelectorAll('*'));const visible=el=>{const cs=getComputedStyle(el),box=el.getBoundingClientRect();return cs.display!=='none'&&cs.visibility!=='hidden'&&Number(cs.opacity||1)>0&&box.width>0&&box.height>0;};const found={};for(const label of labels){const node=elements.find(el=>(el.textContent||'').trim()===label&&visible(el));found[label]=!!node;}return{ok:labels.every(l=>found[l]),missing:labels.filter(l=>!found[l]),labels:Object.keys(found).filter(k=>found[k])};},{rootSelector,labels});}
async function role(page,roleName){const result=await page.evaluate(target=>{const allowed=Orbit.session&&Orbit.session.allowedRoles?Orbit.session.allowedRoles():[];const sel=document.getElementById('rol-sel');if(!allowed.includes(target))return{ok:false,count:allowed.length,active:Orbit.session&&Orbit.session.rol?Orbit.session.rol():''};if(sel){const o=Array.from(sel.options||[]).find(x=>String(x.value||'')===target||String(x.textContent||'').trim()===target);if(o){sel.value=o.value;sel.dispatchEvent(new Event('change',{bubbles:true}));return{ok:true,via:'selector'};}}return{ok:Boolean(Orbit.session&&Orbit.session.set&&Orbit.session.set(target)),via:'session'};},roleName);need(result&&result.ok,'ROLE_SELECTION_FAILED',roleName);await page.waitForFunction(target=>window.Orbit&&Orbit.session&&Orbit.session.rol&&Orbit.session.rol()===target,roleName,{timeout:15000});await page.waitForTimeout(350);}
async function policyRouteDiagnostics(page){return page.evaluate(()=>{const host=document.getElementById('host');const text=host&&host.innerText||'';const headers=host?Array.from(host.querySelectorAll('table.tbl thead th')).map(x=>(x.textContent||'').trim()):[];const premiumHeader=host?Array.from(host.querySelectorAll('table.tbl thead th')).find(x=>(x.textContent||'').trim()==='Prima total'):null;let premiumHeaderVisible=false,premiumHeaderBox=null;if(premiumHeader){const cs=getComputedStyle(premiumHeader),box=premiumHeader.getBoundingClientRect();premiumHeaderBox={width:Math.round(box.width),height:Math.round(box.height)};premiumHeaderVisible=cs.display!=='none'&&cs.visibility!=='hidden'&&Number(cs.opacity||1)>0&&box.width>0&&box.height>0;}let role='',canSee=null,accessCan=null,routeKey='';try{role=Orbit.session&&Orbit.session.rol?Orbit.session.rol():'';}catch(e){}try{canSee=Orbit.session&&Orbit.session.canSee?Orbit.session.canSee('polizas'):null;}catch(e){}try{accessCan=Orbit.access&&Orbit.access.can?Orbit.access.can('polizas','view'):null;}catch(e){}try{routeKey=Orbit.route&&Orbit.route.key||'';}catch(e){}return{hash:location.hash,routeKey,activeRole:role,sessionCanSeePolizas:canSee,accessCanPolizas:accessCan,rows:host?host.querySelectorAll('table.tbl tbody tr').length:0,headers,hasPremiumHeader:headers.includes('Prima total'),premiumHeaderVisible,premiumHeaderBox,hasPremiumLabelInInnerText:text.includes('Prima total'),hasPaginationText:text.includes('Mostrando'),accessDenied:/No tienes acceso con el rol activo/.test(text)};});}

let browser,page;
try{
  need(/^https:\/\//.test(url),'LAB_URL_REQUIRED');need(email.includes('@'),'LAB_EMAIL_REQUIRED');need(password.length>=8,'LAB_PASSWORD_REQUIRED');
  browser=await chromium.launch({headless:true});page=await browser.newPage({viewport:{width:1440,height:1000}});page.on('pageerror',e=>report.pageErrors.push(clean(e&&e.message||e)));
  const entry=new URL(url);entry.searchParams.set('orbitBackend','firestore-lab');entry.searchParams.set('tenant','alianzas-soluciones');
  report.stage='navigate';await page.goto(entry.toString(),{waitUntil:'domcontentloaded',timeout:45000});await page.locator('#login-form').waitFor({state:'visible',timeout:20000});
  report.stage='login';await page.fill('#lg-user',email);await page.fill('#lg-pass',password);await page.click('#login-form button[type="submit"]');await page.waitForFunction(()=>window.Orbit&&Orbit.store&&typeof Orbit.store.all==='function',undefined,{timeout:30000});
  report.stage='blocking_gates';const gates=await settleBlockingGates(page,{arrivalWindowMs:1500,quietWindowMs:500,pollMs:80,hardTimeoutMs:6000,detachTimeoutMs:12000});need(gates&&gates.ok&&gates.remaining===0,'BLOCKING_GATE_NOT_SETTLED');
  report.stage='hydrate';await page.waitForFunction(expected=>{if(!window.Orbit||!Orbit.store)return false;for(const [n,v] of Object.entries(expected)){if((Orbit.store.all(n)||[]).length!==v)return false;}const p=Orbit.store._receiptsPortfolioProjectionStatus&&Orbit.store._receiptsPortfolioProjectionStatus();return !!(p&&p.ready&&p.counts&&p.counts.recibosEsperados===expected.recibosEsperados&&p.counts.carteraPrimas===expected.carteraPrimas);},EXPECT,{timeout:90000,polling:250});
  report.counts=await page.evaluate(names=>Object.fromEntries(Object.keys(names).map(n=>[n,(Orbit.store.all(n)||[]).length])),EXPECT);need(JSON.stringify(report.counts)===JSON.stringify(EXPECT),'BASELINE_COUNTS_INVALID');
  report.stage='samples';const samples=await page.evaluate(()=>{const policies=Orbit.store.all('polizas')||[],vehicles=Orbit.store.all('vehiculos')||[],receipts=Orbit.store.all('recibosEsperados')||[],portfolio=Orbit.store.all('carteraPrimas')||[];const pmap=new Map(policies.map(p=>[p.id,p]));const rmap=new Map(receipts.map(r=>[r.id,r]));const active=p=>p&&(p.estado==='Vigente'||p.estado==='Por renovar');const rich=receipts.find(r=>active(pmap.get(r.polizaId))&&r.primaNeta!=null&&r.gastosExpedicion!=null&&r.gastosFinanciamiento!=null&&r.impuestosIVA!=null&&r.primaTotal!=null)||receipts.find(r=>active(pmap.get(r.polizaId)));const reported=receipts.find(r=>String(r.estadoOperativo||'')==='pago_reportado');const reconciledBalanceRow=portfolio.find(c=>{const source=String(c.fuenteAutoridad||'').trim().toLowerCase();return c&&c.reciboId&&rmap.has(c.reciboId)&&source&&source!=='siga'&&c.matchQuality&&c.sourceRef&&c.requiereValidacion!==true;});const reconciledBalance=reconciledBalanceRow?rmap.get(reconciledBalanceRow.reciboId):null;const veh=vehicles.find(v=>{const p=pmap.get(v.polizaId);return p&&p.clienteId&&v.clienteId&&p.clienteId===v.clienteId;})||vehicles[0];let delta=null;const byPolicy=new Map();for(const r of receipts){if(!byPolicy.has(r.polizaId))byPolicy.set(r.polizaId,[]);byPolicy.get(r.polizaId).push(r);}for(const p of policies){if(!active(p))continue;const rows=byPolicy.get(p.id)||[];if(!rows.length||p.primaTotal==null)continue;const vals=rows.map(r=>Number(r.primaTotal)).filter(Number.isFinite);if(vals.length!==rows.length)continue;const d=vals.reduce((a,b)=>a+b,0)-Number(p.primaTotal);if(Number.isFinite(d)&&Math.abs(d)>=0.005){delta={clienteId:p.clienteId,polizaId:p.id};break;}}const pack=r=>r?{receiptId:r.id,clienteId:r.clienteId,polizaId:r.polizaId}:null;return{rich:pack(rich),reported:pack(reported),reconciledBalance:pack(reconciledBalance),vehicle:veh?{vehicleId:veh.id,clienteId:veh.clienteId,polizaId:veh.polizaId}:null,delta};});need(samples.rich&&samples.reported&&samples.reconciledBalance&&samples.vehicle,'RUNTIME_SAMPLE_UNAVAILABLE');
  report.stage='bind_direction_role';await role(page,'Dirección');report.metrics.activeRoleForHumanReview=await page.evaluate(()=>Orbit.session&&Orbit.session.rol?Orbit.session.rol():'');
  report.stage='global_policies';let t0=Date.now();await route(page,'#/polizas');
  try{await page.waitForFunction(()=>{const host=document.getElementById('host');if(!host||!window.Orbit||!Orbit.route||Orbit.route.key!=='polizas')return false;const rows=host.querySelectorAll('table.tbl tbody tr').length;const th=Array.from(host.querySelectorAll('table.tbl thead th')).find(x=>(x.textContent||'').trim()==='Prima total');if(!th)return false;const cs=getComputedStyle(th),box=th.getBoundingClientRect();const visible=cs.display!=='none'&&cs.visibility!=='hidden'&&Number(cs.opacity||1)>0&&box.width>0&&box.height>0;return rows>0&&rows<=100&&visible&&(host.innerText||'').includes('Mostrando');},undefined,{timeout:12000,polling:100});}catch(e){const diag=await policyRouteDiagnostics(page);Object.assign(report.metrics,{globalPoliciesRouteDiagnostics:diag});need(false,'POLICIES_RENDER_TIMEOUT');}
  const pstate=await policyRouteDiagnostics(page);report.metrics.globalPoliciesRenderMs=Date.now()-t0;report.metrics.globalPoliciesRows=pstate.rows;report.metrics.globalPoliciesHasPremiumHeader=pstate.hasPremiumHeader;report.metrics.globalPoliciesPremiumHeaderVisible=pstate.premiumHeaderVisible;report.metrics.globalPoliciesPremiumHeaderBox=pstate.premiumHeaderBox;report.metrics.globalPoliciesInnerTextIncludesPremium=pstate.hasPremiumLabelInInnerText;report.metrics.globalPoliciesHasPaginationText=pstate.hasPaginationText;report.metrics.globalPoliciesRole=pstate.activeRole;report.metrics.globalPoliciesAccessCan=pstate.accessCanPolizas;report.metrics.globalPoliciesRouteKey=pstate.routeKey;
  need(pstate.routeKey==='polizas','POLICIES_ROUTER_NOT_SYNCHRONIZED');need(pstate.rows>0&&pstate.rows<=100,'POLICIES_DOM_NOT_PAGINATED',String(pstate.rows));need(pstate.hasPremiumHeader&&pstate.premiumHeaderVisible&&pstate.hasPaginationText,'POLICIES_RENDER_CONTRACT_INCOMPLETE');need(pstate.accessCanPolizas===true,'POLICIES_ACCESS_NOT_GRANTED_FOR_DIRECTION');need(report.pageErrors.length===0,'PAGE_ERROR_GLOBAL_POLICIES',report.pageErrors[0]||'');report.checks.globalPoliciesResponsive=true;report.checks.globalPoliciesPaginated=true;report.checks.globalPoliciesDirectionRole=true;report.checks.globalPoliciesPremiumHeaderVisible=true;
  report.stage='policy_fullpage';await route(page,`#/cliente360?c=${samples.rich.clienteId}&p=${samples.rich.polizaId}`);await page.locator('[data-policy-fullpage="1"]').waitFor({state:'visible',timeout:12000});let txt=await bodyText(page);for(const label of ['Prima neta','Gastos de expedición','Gastos financieros','Descuento / ajuste (campo fuente)','IVA / impuestos','Prima total de póliza','Total calendario de recibos'])need(txt.includes(label),'POLICY_PREMIUM_COMPONENT_MISSING',label);need(!/\bundefined\b|\bNaN\b/.test(txt),'TECHNICAL_VALUE_VISIBLE_POLICY');need(!(await technicalCopy(page)),'TECHNICAL_COPY_VISIBLE_POLICY');const qstate=await page.evaluate(pid=>{const p=Orbit.store.get('polizas',pid)||{},v=(Orbit.store.all('vehiculos')||[]).find(x=>x.polizaId===pid)||null,o=Orbit.policyVehicleReadModelV1199c;return{gaps:o&&o.policyCompleteness?o.policyCompleteness(p,v):[],text:(document.getElementById('host')&&document.getElementById('host').innerText)||''};},samples.rich.polizaId);if(qstate.gaps.length)need(qstate.text.includes('Información pendiente de completar'),'POLICY_QUALITY_NOT_FAIL_CLOSED');report.checks.policyPremiumSemantic=true;report.checks.policyQualityFailClosed=true;
  if(samples.delta){report.stage='policy_delta';await route(page,`#/cliente360?c=${samples.delta.clienteId}&p=${samples.delta.polizaId}`);await page.locator('[data-policy-fullpage="1"]').waitFor({state:'visible',timeout:12000});txt=await bodyText(page);need(txt.includes('Diferencia póliza vs calendario'),'POLICY_SCHEDULE_DELTA_NOT_VISIBLE');report.checks.policyScheduleDeltaVisible=true;}else report.checks.policyScheduleDeltaVisible='sample_not_available';
  report.stage='vehicle_fullpage';await route(page,`#/cliente360?c=${samples.vehicle.clienteId}&v=${samples.vehicle.vehicleId}`);await page.locator('[data-vehicle-fullpage="1"]').waitFor({state:'visible',timeout:12000});const vehicleLabels=await visibleSemanticLabels(page,'[data-vehicle-fullpage="1"]',['Detalle completo del vehículo','Placa','Chasis / VIN','Motor','Suma asegurada']);report.metrics.vehicleSemanticLabels=vehicleLabels;need(vehicleLabels.ok,'VEHICLE_DETAIL_FIELD_MISSING',vehicleLabels.missing.join(','));txt=await bodyText(page);need(!/\bundefined\b|\bNaN\b/.test(txt),'TECHNICAL_VALUE_VISIBLE_VEHICLE');report.checks.vehicleFullPage=true;report.checks.vehicleSemanticFieldsVisible=true;
  report.stage='receipt_detail';await route(page,`#/cliente360?c=${samples.rich.clienteId}&t=recibos`);await page.locator('#rp-v910-policy').waitFor({state:'visible',timeout:12000});const row=page.locator(`[data-rp-receipt-id="${samples.rich.receiptId}"]`);await row.waitFor({state:'visible',timeout:12000});await row.click();await page.locator('[data-rp-receipt-detail="1"]').waitFor({state:'visible',timeout:12000});const receiptLabels=await visibleSemanticLabels(page,'[data-rp-receipt-detail="1"]',['Desglose del recibo','Prima neta','Gastos de expedición','Gastos financieros','Descuento / ajuste (campo fuente)','IVA / impuestos','Prima total','Estado y conciliación','Trazabilidad']);report.metrics.receiptSemanticLabels=receiptLabels;need(receiptLabels.ok,'RECEIPT_DETAIL_FIELD_MISSING',receiptLabels.missing.join(','));txt=await bodyText(page);need(!/\bundefined\b|\bNaN\b/.test(txt),'TECHNICAL_VALUE_VISIBLE_RECEIPT');report.checks.receiptDetail=true;report.checks.receiptSemanticFieldsVisible=true;
  report.stage='reported_receipt';await route(page,`#/cliente360?c=${samples.reported.clienteId}&t=recibos`);await page.locator('#rp-v910-policy').waitFor({state:'visible',timeout:12000});const reportedRow=page.locator(`[data-rp-receipt-id="${samples.reported.receiptId}"]`);await reportedRow.waitFor({state:'visible',timeout:12000});await reportedRow.click();await page.locator('[data-rp-receipt-detail="1"]').waitFor({state:'visible',timeout:12000});txt=await bodyText(page);need(/evidencia de pago reportado/i.test(txt)&&/no es un cobro conciliado/i.test(txt),'PAYMENT_REPORTED_NOT_SEPARATED_FROM_COBRO');report.checks.reportedPaymentNotCobro=true;
  report.stage='reconciled_portfolio';await route(page,`#/cliente360?c=${samples.reconciledBalance.clienteId}&t=recibos`);await page.locator('#rp-v910-policy').waitFor({state:'visible',timeout:12000});const reconciledRow=page.locator(`[data-rp-receipt-id="${samples.reconciledBalance.receiptId}"]`);await reconciledRow.waitFor({state:'visible',timeout:12000});await reconciledRow.click();await page.locator('[data-rp-receipt-detail="1"]').waitFor({state:'visible',timeout:12000});txt=await bodyText(page);need(txt.includes('Cartera conciliada con aseguradora'),'PORTFOLIO_RECONCILIATION_LABEL_NOT_VISIBLE');need(/no equivale a un pago/i.test(txt),'PORTFOLIO_CONFUSED_WITH_PAYMENT');need(txt.includes('Fuente autoridad')&&txt.includes('Calidad de match')&&txt.includes('Referencia fuente'),'PORTFOLIO_TRACE_NOT_VISIBLE');report.checks.portfolioReconciledVisible=true;report.checks.portfolioNotPayment=true;
  need(report.counts.cobros===0&&report.counts.finmovs===0,'DOWNSTREAM_COLLECTIONS_NOT_ZERO');need(report.pageErrors.length===0,'PAGE_ERROR_RUNTIME',report.pageErrors[0]||'');need(!(await technicalCopy(page)),'TECHNICAL_COPY_VISIBLE_FINAL');report.checks.downstreamZero=true;report.checks.noPageErrors=true;report.checks.noTechnicalCopy=true;
  report.stage='final';report.ok=true;report.status='HUMAN_REVIEW_VISUAL_LAB_PASS';
}catch(error){report.ok=false;report.status='HUMAN_REVIEW_VISUAL_LAB_FAIL';report.failureStage=report.stage;report.errorCode=String(error&&error.orbitCode||'UNCLASSIFIED_RUNTIME_ERROR');report.error=clean(error&&error.message||error);process.exitCode=41;}finally{if(browser)await browser.close().catch(()=>{});save();}
