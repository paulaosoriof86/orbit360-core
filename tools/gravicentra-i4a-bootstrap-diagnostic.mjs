import fs from 'node:fs';
import path from 'node:path';
import process from 'node:process';
import { chromium } from 'playwright';
import { initializeApp, cert, deleteApp } from 'firebase-admin/app';
import { getAuth } from 'firebase-admin/auth';
import { getFirestore } from 'firebase-admin/firestore';

const PROJECT='ays-orbit-360-lab',TENANT='alianzas-soluciones',PREVIEW=String(process.env.PREVIEW_URL||'').replace(/\/$/,'');
const SOURCE=String(process.env.SOURCE_SHA||''),BUILD=String(process.env.BUILD_ID||''),OUT=process.env.I4A_DIAG_EVIDENCE_DIR||process.env.RUNNER_TEMP||process.cwd();
const clean=v=>String(v==null?'':v).trim();
function canon(v){const k=clean(v).toLowerCase().replace(/\s+/g,' ');return ({'dirección':'Dirección','direccion':'Dirección','superadmin':'SuperAdmin','super admin':'SuperAdmin','admin':'AdminTenant','administrador':'AdminTenant','admintenant':'AdminTenant','operativo':'Operativo','operaciones':'Operativo','asesor':'Asesor'})[k]||clean(v);}
function roles(m){const x=Array.isArray(m?.roles)?m.roles:Array.isArray(m?.rolesAsignados)?m.rolesAsignados:(m?.role||m?.rol?[m.role||m.rol]:[]);return [...new Set(x.map(canon).filter(Boolean))];}
function active(m,rs){return canon(m?.activeRole||m?.rolActivo||m?.defaultRole||m?.rolDefault||m?.roleDefault||rs[0]);}
function sa(){for(const raw of [process.env.SA_DEFAULT,process.env.SA_ORBIT360_LAB,process.env.SA_ORBIT_360_LAB].filter(Boolean)){try{const x=JSON.parse(raw);if(x?.type==='service_account'&&x?.project_id===PROJECT&&x?.client_email&&x?.private_key)return x;}catch{}}throw new Error('SERVICE_ACCOUNT_UNAVAILABLE');}
const app=initializeApp({credential:cert(sa()),projectId:PROJECT},'gravicentra-i4a-diag-v3'),auth=getAuth(app),db=getFirestore(app);
const ms=await db.collection('tenants').doc(TENANT).collection('members').get(),ul=await auth.listUsers(1000),users=new Map(ul.users.map(u=>[u.uid,u])),pool=[];
for(const d of ms.docs){const m=d.data()||{},uid=clean(m.uid||d.id),u=users.get(uid),rs=roles(m);if(!u||u.disabled||u.emailVerified!==true||clean(m.status||m.estado).toLowerCase()!=='active')continue;pool.push({uid,roles:rs,active:active(m,rs)});}
const target=pool.find(x=>x.active==='Dirección'&&x.roles.includes('Dirección'))||pool.find(x=>x.active==='SuperAdmin')||pool[0];
fs.mkdirSync(OUT,{recursive:true});const ev={schemaVersion:'gravicentra-i4a-bootstrap-diagnostic-v3',gate:'I4A',purpose:'isolate-ready-listener-session-latency',sourceSha:SOURCE,buildId:BUILD,previewUrl:PREVIEW,productionTouched:false,dataTouched:false,writesExecuted:0,userIdentitiesRecorded:false,tokensRecorded:false,cases:{},errors:[]};
async function one(browser,name){const token=await auth.createCustomToken(target.uid,{gravicentraI4AReadOnly:true}),ctx=await browser.newContext({viewport:{width:1440,height:1000}}),page=await ctx.newPage(),rec={activeRole:target.active,roleCount:target.roles.length};ev.cases[name]=rec;try{await page.goto(PREVIEW,{waitUntil:'domcontentloaded',timeout:20000});await page.waitForFunction(()=>!!Orbit?.productAppP0&&!!Orbit?.productRuntimeBrowserProvidersP0&&!!Orbit?.session?.syncFromAuth,null,{timeout:5000});const out=await page.evaluate(async tok=>{
 const round=n=>Math.round(Number(n||0)*10)/10;window.__timing={dispatch:[],sync:[],storage:[]};
 const nativeDispatch=EventTarget.prototype.dispatchEvent;EventTarget.prototype.dispatchEvent=function(ev){const tracked=ev&&['orbit:product-readonly-bootstrap','orbit:product-app','orbit:session','orbit:session:blocked','orbit:auth','orbit:store','hashchange'].includes(ev.type),phase=ev?.detail?.phase||'',t0=performance.now();try{return nativeDispatch.call(this,ev);}finally{if(tracked)window.__timing.dispatch.push({type:ev.type,phase,ms:round(performance.now()-t0)});}};
 const nativeSet=Storage.prototype.setItem,nativeRemove=Storage.prototype.removeItem,nativeGet=Storage.prototype.getItem;Storage.prototype.setItem=function(k,v){const t=performance.now();try{return nativeSet.call(this,k,v);}finally{if(String(k).startsWith('orbit360_'))window.__timing.storage.push({op:'set',key:String(k),ms:round(performance.now()-t)});}};Storage.prototype.removeItem=function(k){const t=performance.now();try{return nativeRemove.call(this,k);}finally{if(String(k).startsWith('orbit360_'))window.__timing.storage.push({op:'remove',key:String(k),ms:round(performance.now()-t)});}};Storage.prototype.getItem=function(k){const t=performance.now();try{return nativeGet.call(this,k);}finally{if(String(k).startsWith('orbit360_'))window.__timing.storage.push({op:'get',key:String(k),ms:round(performance.now()-t)});}};
 const old=Orbit.session,nativeSync=old.syncFromAuth;Orbit.session=Object.freeze(Object.assign({},old,{syncFromAuth:function(){const t=performance.now();try{return nativeSync.apply(old,arguments);}finally{window.__timing.sync.push({ms:round(performance.now()-t)});}}}));
 const p=Orbit.productRuntimeBrowserProvidersP0,c=await p.initialize();await c.modules.auth.signInWithCustomToken(c.auth,tok);const started=performance.now();let ok=true,error='';try{await Orbit.productAppP0.activate();}catch(e){ok=false;error=String(e?.message||e);}return{ok,error,durationMs:Math.round(performance.now()-started),timing:window.__timing,app:Orbit.productAppP0.status()};
 },token);Object.assign(rec,out);}catch(e){rec.harnessError=String(e?.message||e);ev.errors.push(name+':'+rec.harnessError);}finally{await ctx.close();}}
let browser;try{if(!target)throw new Error('NO_ACTIVE_TARGET');browser=await chromium.launch({headless:true});await one(browser,'session-latency-1');if(ev.cases['session-latency-1']?.ok!==true)await one(browser,'session-latency-2');}catch(e){ev.errors.push(String(e?.message||e));process.exitCode=1;}finally{if(browser)await browser.close().catch(()=>{});await deleteApp(app).catch(()=>{});fs.writeFileSync(path.join(OUT,'i4a-bootstrap-diagnostic.json'),JSON.stringify(ev,null,2)+'\n');console.log(JSON.stringify(ev,null,2));}
